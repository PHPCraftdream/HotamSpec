// Code generated by .scratch/selfspec-codegen (task #344, RAC-0). DO NOT EDIT BY HAND.
// Regenerate by re-running the throwaway codegen program against the current
// domains/hotam-spec-self/graph.json if this pilot subset ever needs to change.
//
// STRUCTURAL fields only: Claim, Why, Owner, Status, Relations, Assumptions,
// Enforcement, EnforcedBy, Enforceability, MTag, Summary, CreatedAt, SettledAt,
// BlockedOn, ImplementedBy, VerifiedBy, SourceRefs, DeclOrder. EVENT fields
// (History, GateSignoffs, LastReviewedAt, ReviewAfter, Evidence) are
// deliberately absent — MergeIntoGraph passes them through from the graph's
// existing node untouched.
package selfspec

import "github.com/PHPCraftdream/HotamSpec/internal/ontology"

var _ = Requirements.MustRegister("R-agent-imports-framework", ontology.Requirement{
	ID:             "R-agent-imports-framework",
	Claim:          "An agent's code shall import the framework body (`hotam_spec.*`) as shared infrastructure; the framework body is owned by no single agent.",
	Owner:          "framework-author",
	Status:         "REJECTED",
	Why:            "REJECTED -- REPLACES split into R-agent-code-imports-framework + R-framework-owned-by-no-agent (Wave 2 burn-down, decided by framework-author 2026-07-02) per atomicity discipline (R-requirement-claim-is-atomic). audit_atomicity.py flagged this claim COMPOUND (semicolon splits 2 segments): (1) an agent's code shall import hotam_spec.* as shared infrastructure, and (2) the framework body is owned by no single agent. These are two distinct concerns -- (1) is a mechanically checkable import-direction fact about agent source files, (2) is a non-code ownership/governance stance about the framework body. Splitting lets (1) reach a real enforcer (a static AST import-direction scan, mirroring R-core-imports-stdlib-or-hotam-spec-only's pattern) without diluting it with the unenforceable ownership half. — (was: Keeps the framework content-free (R-content-free-framework) while letting agents specialize. BUILD-TRIGGER: same as R-agent-is-a-directory.)",
	Assumptions:    []string{"A-content-free-honest"},
	Relations:      []ontology.Relation{},
	Enforcement:    "PROSE",
	EnforcedBy:     []string{},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "",
	SourceRefs:     []string{},
	DeclOrder:      90,
})

var _ = Requirements.MustRegister("R-ai-presents-not-decides", ontology.Requirement{
	ID:             "R-ai-presents-not-decides",
	Claim:          "The AI agent shall NEVER close a Conflict silently -- it presents with justification and defers every resolution to the human resolver.",
	Owner:          "ai-agent",
	Status:         "SETTLED",
	Why:            "The hard boundary. If the AI resolves contradictions itself, invisibility returns — now AI-created. Made structural by check_resolver_not_a_member_owner.",
	Assumptions:    []string{"A-stakeholders-care"},
	Relations:      []ontology.Relation{},
	Enforcement:    "STRUCTURAL",
	EnforcedBy:     []string{},
	MTag:           "",
	Enforceability: "INHERENTLY_PROSE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-06-30",
	SourceRefs:     []string{},
	DeclOrder:      5,
})

var _ = Requirements.MustRegister("R-anchor-everything", ontology.Requirement{
	ID:             "R-anchor-everything",
	Claim:          "Every object shall carry a stable, short, typed anchor (prefix names the kind: R-/C-/A-/OP-/GOAL-/...).",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "SETTLED: structurally enforced via independent checks in internal/invariants. check_typed_anchors_* (typed_anchors.go) fire when any R-/A-/C-/OP-/GOAL-/PR-/entity id lacks its typed prefix; check_section_anchors_known (self_reference.go) fires when any §-token in framework docstrings is absent from the glossary. Together these make the anchor discipline machine-checkable at every invariant run.",
	Assumptions:    []string{"A-bootstrap-self-applies"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_typed_anchors_requirement", "check_typed_anchors_assumption", "check_typed_anchors_conflict", "check_typed_anchors_operator", "check_typed_anchors_process", "check_typed_anchors_goal", "check_typed_anchors_entity", "check_section_anchors_known"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-07-12",
	SourceRefs:     []string{"internal/invariants/typed_anchors.go", "internal/invariants/self_reference.go"},
	DeclOrder:      51,
})

var _ = Requirements.MustRegister("R-authored-prose-no-live-tallies", ontology.Requirement{
	ID:             "R-authored-prose-no-live-tallies",
	Claim:          "Authored prose fields that narrate a domain's DURABLE rationale or description -- Process.Why/Step.Why and manifest.json's `goals`/`charter` -- MUST NOT carry a point-in-time status snapshot (a snapshot-marker phrase co-occurring with an ISO date, e.g. \"текущее положение\"/\"по состоянию на\"/\"as of\"/\"current status\" plus a dated claim; or a live \"N из/of M\" tally co-occurring with a stage token from the domain's OWN declared `gate_stage_order`). A live count or dated status claim belongs to a generated projection instead -- PIPELINE.md's generated \"Live state\" section, DOMAIN-MAP's gates line, or an assert-checked orientation_faq phrase template -- never frozen into prose that nothing regenerates. Requirement.Claim and Conflict.Context are DELIBERATELY OUT of this claim's scope for now: they carry more legitimate risk of false positives (a Requirement's claim text can legitimately assert a count or a status word as part of its own normative substance, unlike why/goals/charter's narrower status-narrative role) -- a future requirement MAY extend this discipline there if a design consult finds the same two predicates stay low-noise against that register.",
	Owner:          "framework-author",
	Status:         "DRAFT",
	Why:            "Task #333 (R4F-prose-lint), fourth external review §4.1 synthesis: the \"live numbers baked into durable authored prose\" smell task #331 found and fixed for Process.Why/Step.Why (prat/gpsm-sm's Process.Why literally read \"27 из 32 ФТ... ТЕКУЩЕЕ ПОЛОЖЕНИЕ на 2026-07-21\" while the graph had since moved to 32/32) is not unique to that one field -- it is a CLASS affecting any authored text field. Confirmed live in the same sibling domain: gpsm-sm's manifest.json `goals` field, before task #329's rewording, baked in an identical \"32/32 SIGNED\"-shaped snapshot. This requirement generalizes #331's engine-portion fix. `internal/invariants/authored_prose_snapshot.go`'s `checkAuthoredProseSnapshot` (renamed from `process_why_snapshot.go`'s `checkProcessWhySnapshotProse`, exported as `AuthoredProseSnapshotWarnings`, replacing `ProcessWhySnapshotWarnings` -- wired into `cmd/hotam`'s non-blocking ADVISORY section exactly as before, `all_violations.go:printAdvisorySection`, never registered in the `All` registry) now ALSO scans manifest.json's `goals` (each list entry) and `charter` (a single string), resolved via `loader.ResolveDomainPresentation` -- the identical loader already used by the DOMAIN-MAP/PROJECT-ESSENCE renderers -- with the EXACT SAME two predicates #331 established, no broadening of the pattern set. Verified against both real sibling-repo consumer manifests (read-only inspection, PRAT-hotam is never edited by this engine-side task): `domains/gpsm-sm/manifest.json`'s CURRENT (post-#329) goals/charter text produces zero violations (the rewording that removed the hardcoded tally is exactly the fix this check exists to push toward), and a reconstructed pre-#329-shaped goals sentence (a marker phrase + ISO date, mirroring the real historical smell) DOES fire, proving the check has real teeth. `domains/prat/manifest.json`'s goals/charter also produce zero violations. A scan of `hotam-spec-self`'s own 297-requirement graph found zero real fires of the two precise predicates against `Requirement.Claim`/`Conflict.Context` text (so extending there would currently be a safe no-op), but also found roughly a dozen claims pairing a bare digit with a status word in ordinary normative prose (e.g. a requirement legitimately describing a command's behavior using a count) -- evidence that the CLAIM/CONTEXT register is noisier than why/goals/charter's narrower \"narrate current standing\" role, so this requirement's claim text explicitly declines to extend there for now, leaving the door open to a future design consult.",
	Assumptions:    nil,
	Relations:      []ontology.Relation{{Kind: "refines", Target: "R-domain-overview-projection"}},
	Enforcement:    "PROSE",
	EnforcedBy:     []string{"check_authored_prose_snapshot"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-23",
	SettledAt:      "",
	SourceRefs:     []string{"internal/invariants/authored_prose_snapshot.go", "internal/loader/loader.go", "cmd/hotam/all_violations.go"},
	DeclOrder:      0,
	ImplementedBy:  []string{"internal/invariants/authored_prose_snapshot.go:checkAuthoredProseSnapshot"},
	VerifiedBy:     []string{"internal/invariants/authored_prose_snapshot_test.go:TestCheckAuthoredProseSnapshot_FiresOnManifestGoalsPreTask329Shape", "internal/invariants/authored_prose_snapshot_test.go:TestCheckAuthoredProseSnapshot_TrueNegativeOnRealPostTask329GpsmSmGoals"},
})

var _ = Requirements.MustRegister("R-claude-md-current", ontology.Requirement{
	ID:             "R-claude-md-current",
	Claim:          "A domain's committed root or local CLAUDE.md, once the crystal convention is adopted for its project, shall keep its generated portion (everything up to and including the durable-notes marker line) byte-identical to a fresh hotam gen-spec render of the current graph, mechanically checked by check_domain_claude_md_current; content the operator authors below that marker line is outside this check's scope.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "External review P1: check_spec_md_current closed the staleness gap for docs/gen/SPEC.md, but CLAUDE.md -- the resident boot crystal every operator session actually reads first -- had no equivalent freshness invariant at all. check_domain_claude_md_current (internal/invariants/claude_md_current.go, real implementation wired in from cmd/hotam/claude_md_current_wiring.go via registry.Update -- the same pattern tool_wiring.go already establishes for methodology.Tools' Run field, needed here because internal/invariants must never import internal/generator, a real, mechanically-enforced import cycle) closes that gap by comparing the committed crystal's generated span against a fresh render on every all-violations pass, ignoring the durable-notes tail the template's own text invites operators to write below the marker line. This requirement is the self-hosting anchor (mirrors R-orientation-faq-answerable's and R-scenario-spec-obligations-mechanically-enforced's identical role for their own check_* gates) that satisfies check_bijection_r_to_enforcer for this new check.",
	Assumptions:    nil,
	Relations:      nil,
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_domain_claude_md_current"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-22",
	SettledAt:      "2026-07-22",
	SourceRefs:     nil,
	DeclOrder:      0,
})

var _ = Requirements.MustRegister("R-conflict-is-connector-node", ontology.Requirement{
	ID:             "R-conflict-is-connector-node",
	Claim:          "A contradiction shall be modeled as a first-class Conflict NODE carrying axis + context + shared_assumption + resolver, never as a `conflicts_with` edge between requirements.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "The central ontological insight. An edge holds nothing; a node holds knowledge belonging to neither party (the axis, the context, the shared root) -- that is what makes contradictions first-class and clusterable. REPOINTED 2026-07-02 (Wave 7 move 4, P5 latent-connector cluster fix): assumptions moved from A-content-free-honest (an over-broad, unrelated content-freeness assumption shared by coincidence with two other unrelated requirements, producing a false 3-way latent-connector cluster) to A-conflict-is-a-node-not-an-edge, which names this requirement's actual premise.",
	Assumptions:    []string{"A-conflict-is-a-node-not-an-edge"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_conflict_has_axis", "check_conflict_has_context", "check_conflict_has_resolver"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-07-12",
	SourceRefs:     []string{"internal/ontology/conflict.go", "internal/ontology/graph_smoke_test.go"},
	DeclOrder:      2,
	ImplementedBy:  []string{"internal/ontology/conflict.go:Conflict"},
	VerifiedBy:     []string{"internal/ontology/graph_smoke_test.go:TestConflictPredicates"},
})

var _ = Requirements.MustRegister("R-decided-needs-human-signoff", ontology.Requirement{
	ID:             "R-decided-needs-human-signoff",
	Claim:          "A Conflict in DECIDED(...) lifecycle shall carry a decided_by: Stakeholder.id field (later: a cryptographic signature) — enforced by a new invariant.",
	Owner:          "framework-reviewer",
	Status:         "SETTLED",
	Why:            "SETTLED (P3): Conflict.decided_by field added; check_decided_has_decided_by fires when lifecycle starts with DECIDED but decided_by is empty or owned by a member. Makes the hard boundary structural at the decision moment — the AI cannot silently write DECIDED without naming a human decider who is outside the conflict's members.",
	Assumptions:    []string{"A-stakeholders-care"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_decided_has_nonempty_decided_by", "check_decided_by_is_known_stakeholder", "check_decided_by_not_member_owner"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-06-30",
	SourceRefs:     []string{},
	DeclOrder:      22,
})

var _ = Requirements.MustRegister("R-gate-cohort-explicit-denominator", ontology.Requirement{
	ID:             "R-gate-cohort-explicit-denominator",
	Claim:          "A `gate_signoff_count` orientation_faq assert (loader.OrientationFAQAssert) that ALSO declares a manifest-level `gate_cohort` ({\"statuses\": [...], \"exclude\": [...]}) answers a STRONGER question than the assert's pre-existing bare form: 'have ALL cohort requirements passed this stage' -- a cohort member that was NEVER ASSESSED at the target stage (no GateSignoff record at all, neither SIGNED nor DEFERRED) MUST count AGAINST `expect:\"all\"`, never be silently invisible to it. Absent a declared `gate_cohort`, the assert's denominator stays `total = tally.Signed + tally.Deferred` (today's semantics, byte-identical -- a Requirement never evaluated at the stage is invisible to that sum by construction, and this weaker form remains valid for a domain that has not opted into an explicit cohort declaration). Separately, a `gate_signoff_count` assert MUST tally FAIL CLOSED -- refuse to silently conflate distinct pipeline executions -- when its target stage carries GateSignoffs from more than one distinct `pipeline_run` and the assert itself does not declare which `pipeline_run` it means; declaring `pipeline_run` on the assert disambiguates and tallies only that one run's signoffs.",
	Owner:          "framework-author",
	Status:         "DRAFT",
	Why:            "Task #330 (R4-cohort), fourth external review: `gate_signoff_count`'s pre-existing denominator (`tally.Signed + tally.Deferred`) is blind to a Requirement with NO gate-signoff record at all at a stage -- it was never evaluated, so it contributes to neither Signed nor Deferred, and `expect:\"all\"` can pass while that requirement was silently never assessed. Confirmed live in `gpsm-sm`: 35 requirements total, only 32 carry any gate signoff at all at the relevant stage -- the other 3 (`R-domain-exists` REJECTED, `R-gpsm-pg1-passage`/`R-gpsm-project` SETTLED meta-requirements describing the pipeline itself rather than passing through it) are legitimately out of cohort, which is exactly why the simpler alternative (`total = all Requirements` regardless of role) was rejected during this task's design consult in favor of an explicit, domain-declared cohort. `loader.ResolveGateCohort` (`internal/loader/gate_cohort.go`) reads the optional manifest `gate_cohort` field (statuses default `[\"SETTLED\"]` when declared-but-empty); `graphfacts.CohortCount` (`internal/graphfacts/facts.go`) is the trivial counted-filter extraction (matching this package's own 'extracted so divergent reimplementations can't creep' discipline, mirroring `lastSignoffAtStage`'s identical rationale for the gate-tally dedup rule); `evalOrientationAssert` (`internal/invariants/orientation_faq_assert.go`) wires the cohort denominator in when declared, fails closed on an `exclude` id that matches no real Requirement or a `statuses` entry that is not a recognized status (reusing `graphfacts.RequirementStatusTally`'s own exact-match + OPEN-prefix matching rule, not a new one), and also wires the previously-unused `OrientationFAQAssert.State` field (`\"\"`/`\"SIGNED\"` byte-identical default vs `\"DEFERRED\"`). The multi-run half closes a second, related gap: `GateSignoff.PipelineRun` was already mandatory/populated on every signoff but `graphfacts` silently conflated every run's signoffs when tallying a stage -- `lastSignoffAtStage`/`GateSignoffTally` now take a `run` parameter (`\"\"` = all runs = the exact pre-existing behavior, still the default for `internal/generator/pipeline.go`'s Live-state renderer and `internal/generator/claudemd.go`'s DOMAIN-MAP renderer, both unchanged), and a new `graphfacts.PipelineRunsAtStage` plus `OrientationFAQAssert.PipelineRun` field let an assert either disambiguate a genuinely multi-run stage or fail closed when it does not. Actually declaring `gate_cohort` in `gpsm-sm`'s own manifest.json (activating the stronger check there) is intentionally a SEPARATE follow-up, out of this requirement's scope -- this requirement claims only the engine-portion capability now existing and regression-tested.",
	Assumptions:    nil,
	Relations:      []ontology.Relation{{Kind: "refines", Target: "R-orientation-faq-answerable"}},
	Enforcement:    "PROSE",
	EnforcedBy:     nil,
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-23",
	SettledAt:      "",
	SourceRefs:     []string{"internal/loader/gate_cohort.go", "internal/loader/orientation_faq.go", "internal/graphfacts/facts.go", "internal/invariants/orientation_faq_assert.go"},
	DeclOrder:      0,
	ImplementedBy:  []string{"internal/graphfacts/facts.go:CohortCount", "internal/invariants/orientation_faq_assert.go:evalOrientationAssert"},
	VerifiedBy:     []string{"internal/invariants/orientation_faq_assert_test.go:TestCheckOrientationFAQAnswered_GateCohort_NeverEvaluatedMemberFailsAll"},
})

var _ = Requirements.MustRegister("R-gate-signoff-single-carrier", ontology.Requirement{
	ID:             "R-gate-signoff-single-carrier",
	Claim:          "A domain that runs its Requirements through a staged review methodology (e.g. prat/gpsm-sm's P-G0..P-G4 planning gates) has ONE typed carrier on the graph recording per-Requirement, per-stage gate passage -- Requirement.gate_signoffs -- instead of that fact being reconstructed from prose scattered across History entries, manifest goals, and Process why-fields (a drift that has already recurred once in this framework's own consumer domains). Within one Requirement and one pipeline_run, SIGNED gate_signoffs MUST appear in the domain-declared gate_stage_order (a manifest.json opt-in DATA field, never an engine-known enum -- the engine serves domains with no staged-gate discipline at all) -- no stage SIGNED before every earlier stage is SIGNED too. Every DEFERRED gate_signoff MUST carry a non-empty deferred_reason, and when that reason references a Conflict id (C-[0-9a-f]{8}), that id MUST resolve to a real Conflict node in the graph. Every SIGNED gate_signoff MUST carry a populated signoff (decided_by + verbatim) and non-empty evidence, and signoff.decided_by, when present, MUST resolve to a known Stakeholder.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "SETTLED: an external review of the gpsm-sm consumer domain found its 'current gate-passage stage' scattered across graph.json History entries (hand-written after each test run), manifest.json goals, and a Process why-field -- three independent text sources that had already drifted out of sync with each other (fixed by hand in this same wave, task D1). The structural fix is Requirement.GateSignoffs (internal/ontology/gate_signoff.go): a typed, append-only list of {stage, state, deferred_reason, evidence, pipeline_run, signoff} entries, landed exclusively via the new ProposedGateSignoffBatch proposal kind (internal/proposal/types.go) so History entries are AUTO-GENERATED from the structural diff (internal/proposal/history.go's summarizeFieldDiff, the SAME mechanism already used for Requirement/EntityType field updates) rather than hand-written -- closing the exact drift class the review found. Stage is a bare string, not an engine enum: the engine also serves hotam-spec-self and hotam-dev, which have no staged-gate methodology at all, so a domain that wants monotonic-order checking declares gate_stage_order itself (internal/loader/gate_stage_order.go, the same tolerant opt-in-resolver shape as ResolveOrientationFAQ/ResolveDiscipline -- honest no-op for every domain that never declares it, exactly like check_orientation_faq_answered's identical boundary). The three checks -- check_gate_signoff_monotonic, check_gate_signoff_deferred_reason_present, check_gate_signoff_deferred_conflict_resolves (internal/invariants/gate_signoff_checks.go) -- make 'gate passage is monotonic and every deferral has a real, resolvable reason' a mechanically verified property of the graph rather than a convention a resolver must remember to honor by hand across dozens of Requirements. This domain (hotam-spec-self) itself declares no gate_stage_order and carries no gate_signoffs, so all three checks are honest no-ops here -- this requirement exists to anchor the checks for the bijection discipline (R-bijection-r-to-enforcer), not to claim hotam-spec-self uses the feature. Task #319 (R3-signoff-strict, external review) added two more checks anchored here -- check_gate_signoff_signed_has_provenance and check_gate_signoff_decided_by_is_known_stakeholder (internal/invariants/gate_signoff_checks.go): before this task the engine allowed a SIGNED gate_signoff to land with ZERO provenance (no decided_by, no verbatim, no evidence) beyond the bare stage/state/pipeline_run fields -- a real weakness for any domain (e.g. the planned life-domain work) where gate signoffs represent genuine human decisions. Both checks are ongoing all-violations invariants (like check_gate_signoff_deferred_reason_present), not proposal-time-only, mirroring that existing choice; verified safe against prat/gpsm-sm sibling-repo existing landed data (64 SIGNED gate_signoffs, all already carrying full decided_by/verbatim/evidence) before being wired up as ongoing rather than proposal-time-only. This domain (hotam-spec-self) still declares no gate_stage_order and carries no gate_signoffs, so all five checks remain honest no-ops here -- this requirement continues to exist only to anchor the checks for the bijection discipline (R-bijection-r-to-enforcer), not to claim hotam-spec-self uses the feature.",
	Assumptions:    []string{},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_gate_signoff_monotonic", "check_gate_signoff_deferred_reason_present", "check_gate_signoff_deferred_conflict_resolves", "check_gate_signoff_signed_has_provenance", "check_gate_signoff_decided_by_is_known_stakeholder"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-21",
	SettledAt:      "2026-07-21",
	SourceRefs:     []string{"internal/ontology/gate_signoff.go", "internal/loader/gate_stage_order.go", "internal/invariants/gate_signoff_checks.go", "internal/proposal/types.go", "internal/proposal/history.go"},
	DeclOrder:      0,
})

var _ = Requirements.MustRegister("R-generations-inherit-doc-test-code", ontology.Requirement{
	ID:             "R-generations-inherit-doc-test-code",
	Claim:          "Every artifact the methodology generates MUST recursively inherit the doc-test-code form: EVERY EntityType MUST yield a Go struct, its lifecycle methods, and transition tests; EVERY SETTLED requirement MUST yield a named Go test case (a real assertion when formalizable, an honest skip carrying the rule text otherwise).",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "Dogfooding gen-code (PLAN-gen-code.md stage 0): the methodology must apply its own doc-test-code discipline recursively to everything it generates, not only to hand-written code.",
	Assumptions:    nil,
	Relations:      nil,
	Enforcement:    "PROSE",
	EnforcedBy:     nil,
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-15",
	SettledAt:      "2026-07-15",
	SourceRefs:     []string{"PLAN-gen-code.md"},
	DeclOrder:      0,
})

var _ = Requirements.MustRegister("R-glossary-drift-stable", ontology.Requirement{
	ID:             "R-glossary-drift-stable",
	Claim:          "The committed docs/gen/GLOSSARY.md shall equal the regeneration of the current graph byte-for-byte.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "Atom of R-glossary-sync-test (drift-stability concern). Same anti-drift discipline as R-drift-structurally-impossible applied to the glossary.",
	Assumptions:    []string{"A-text-grounded-in-models", "A-python-stack"},
	Relations:      []ontology.Relation{{Kind: "replaces", Target: "R-glossary-sync-test"}},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"TestBuildGlossary_ByteIdenticalToFixture"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-07-12",
	SourceRefs:     []string{},
	DeclOrder:      115,
})

var _ = Requirements.MustRegister("R-no-hand-edit-graph", ontology.Requirement{
	ID:             "R-no-hand-edit-graph",
	Claim:          "Changes to domains/*/graph.json shall be made only through `hotam apply-proposal` / `hotam land`, with direct hand-edits prohibited outside of bootstrap events.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "The closed loop's ACT half goes through the proposal writer (R-active-loop-apply-tool). The sanctioned write path is `hotam apply-proposal <file.json>` or `hotam land` (cmd/hotam/apply_proposal.go, cmd/hotam/land.go), which load a Proposed* JSON, validate it, mutate the in-memory graph, check for new invariant violations, and only then write graph.json + graph.lock (internal/proposal/apply.go). Not yet implemented as a deterministic host-hook guard: the CLI is a pure command-line tool with no PreToolUse / host-hook integration, so there is no mechanism that mechanically blocks direct Edit/Write on graph.json. (Historically the Python _graph_guard.py PreToolUse hook provided that mechanical block.) Without a guard on graph.json, a hand-edit can bypass the proposal protocol's validation+invariant-check gate -- the same self-judging-the-judge problem (Enf#3, supervector A). PROSE (not ENFORCED): no enforcer exists. ENFORCED 2026-07-13: TestNoHandEditGraph_RealDomainLocksPinCurrentGraph in internal/loader/lock_real_domains_test.go mechanically verifies this claim -- but only for THIS repo's own two domains (hotam-spec-self, hotam-dev), and only as a TEST, never as part of `hotam all-violations` itself, so a hand-edited CONSUMER domain graph.json (e.g. an adopting repo's domains/prat, domains/gpsm-sm) reported '0 violations -- graph clean' even with a stale lock (@fh adversarial review finding F6). ENFORCED_BY WIDENED 2026-07-17: check_graph_lock_pins_graph_json (internal/invariants/domain_structure.go) moves the same loader.VerifyLock machinery into the graph-generic invariant registry, keyed off g.DomainDir (populated by loader.LoadGraph for every domain, self-hosting or consumer) so it runs inside `hotam all-violations` itself for ANY domain, closing the gap the test-only enforcer left open. settled_at RESTORED 2026-07-13 from git HEAD 9af0176 (pre-corruption value) after a mutate.go bug this session silently reset it on content-only updates; see task #84.",
	Assumptions:    []string{"A-python-stack"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"TestNoHandEditGraph_RealDomainLocksPinCurrentGraph", "check_graph_lock_pins_graph_json"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-07-12",
	SourceRefs:     []string{"internal/proposal/apply.go", "internal/loader/lock_real_domains_test.go", "internal/invariants/domain_structure.go"},
	DeclOrder:      171,
	ImplementedBy:  []string{"internal/proposal/apply.go:Apply"},
	VerifiedBy:     []string{"internal/loader/lock_real_domains_test.go:TestNoHandEditGraph_RealDomainLocksPinCurrentGraph"},
})

var _ = Requirements.MustRegister("R-orientation-faq-answerable", ontology.Requirement{
	ID:             "R-orientation-faq-answerable",
	Claim:          "An AI agent (or a new human reader) orients in this project fast: for every basic onboarding question a domain DECLARES in its own manifest.json \"orientation_faq\" opt-in list (e.g. what is this project / what is the requirement lifecycle / who decides what / what is currently blocked / where is the full requirements list), the answer MUST be reachable from the domain's generated crystal in at most ONE hop -- NEVER buried behind a chain of pointers the agent would have to follow blind -- and, when the entry declares an \"assert\", the answer MUST ALSO be LIVE-TRUE against the domain's current graph state, not merely a lexical leftover from when the entry was authored. An answer is reachable in <=1 hop when EITHER (a) the manifest entry's declared keywords ALL appear inline in the crystal's text (the answer is present with ZERO hops), OR (b) the manifest entry's declared link (a repo-root-relative path, the SAME convention the crystal's own cross-references use) appears in the crystal's text (as a markdown link OR a bare path) AND resolves to a REAL EXISTING FILE on disk; (a)/(b) are ALTERNATIVES, either satisfies the keywords/link contract. When the entry ALSO declares (c) an \"assert\" (kind: gate_signoff_count | conflict_count_by_lifecycle | requirement_count_by_status, backed by internal/graphfacts's live graph-fact tallies), the assert's live (count, total) pair must satisfy the entry's declared \"expect\" (all / none / {op, value}) and/or its declared \"phrase\" ({count}/{total}-templated, live-substituted, required present in the crystal or linked file) -- this third signal is ADDITIVE, not an alternative: it must pass ALONGSIDE whatever keywords/link contract the entry carries, never substitute for it. A domain that has NOT declared an orientation_faq list makes NO orientation promise and this requirement is an honest no-op for it -- exactly the same opt-in boundary discipline:\"full\" / the committed SPEC.md / the vendored recorder already draw between the engine's structural floor and a domain's own stricter, explicitly-declared contract.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "SETTLED: making 'an agent orients fast' a MECHANICALLY CHECKABLE property (not a hope or a demo-session feeling) is the orientation showcase the framework sells to a customer -- a measurable, drift-proof guarantee that declared onboarding answers cannot silently disappear when a crystal is rewritten, a doc is moved, or a section is reworded. The structural half is check_orientation_faq_answered (internal/invariants/orientation_faq.go), which reads each declared manifest entry via loader.ResolveOrientationFAQ (internal/loader/orientation_faq.go -- the SAME opt-in reader shape ResolveDiscipline/ResolveGenProfile already establish: honest no-op when the field is absent, real per-entry gate when present) and proves each declared question is answerable from the crystal in <=1 hop. The check mirrors the established filesystem-aware, honest-no-op-when-absent shape of check_recorder_current / check_spec_md_current / check_settled_requires_scenario: a domain without an orientation_faq list contributes ZERO violations regardless of how sparse its crystal is ('no committed opt-in = no lie'), and a domain WITH the list is held to its own declared contract. The crystal-path resolution (resolveCrystalPath: local <domainDir>/CLAUDE.md for a consumer domain, else repo-root CLAUDE.md for the active domain) mirrors cmd/hotam.resolveClaudeMDPath's intent via a robust existing-file priority. This domain (hotam-spec-self) carries the real self-example: 5 questions covering purpose / lifecycle / decision-authority / current-blockers / full-requirements-list, each answerable inline (keywords) or via a one-hop link to a real docs/gen file. TASK #321 (R3-semantic-faq) EXTENSION: the original two-signal (keywords/link) rule proves a phrase is lexically PRESENT in the crystal, never that the phrase is still semantically TRUE relative to the graph's current state -- this session hit exactly this bug (a manifest FAQ entry claimed '27 of 32 requirements' and kept passing the keyword check long after the graph reached 32/32, fixed by hand in tasks #318/#322 without closing the underlying design gap). The new optional \"assert\" field (loader.OrientationFAQAssert, internal/loader/orientation_faq.go) ties an entry to a LIVE query over the graph (internal/graphfacts/facts.go: GateSignoffTally/GateFrontier/ConflictLifecycleTally/RequirementStatusTally) instead of, or alongside, the static keyword/link signals -- evalOrientationAssert (internal/invariants/orientation_faq_assert.go) evaluates it and fails closed on any unrecognized kind, undeclared gate stage, malformed expect, or an assert declaring neither expect nor phrase. This claim text was updated (task #321) to describe the new three-signal rule the code now implements; it was NOT applied unilaterally -- per R-decided-needs-human-signoff/R-ai-presents-not-decides this UPDATE is drafted as proposals/draft-R-orientation-faq-answerable-assert.json for human review and separate landing, not applied by the same task that authored the code. The self-hosting hotam-spec-self domain's own orientation_faq entries remain keyword/link-only (zero assert entries) -- migrating them to use assert is explicitly a separate follow-up task, not part of this engine-portion change.",
	Assumptions:    []string{"A-bootstrap-self-applies"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_orientation_faq_answered"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-21",
	SettledAt:      "2026-07-21",
	SourceRefs:     []string{"internal/invariants/orientation_faq.go", "internal/invariants/orientation_faq_assert.go", "internal/loader/orientation_faq.go", "internal/graphfacts/facts.go", "domains/hotam-spec-self/manifest.json"},
	DeclOrder:      0,
})

var _ = Requirements.MustRegister("R-pipeline-live-state-from-typed-carriers", ontology.Requirement{
	ID:             "R-pipeline-live-state-from-typed-carriers",
	Claim:          "PIPELINE.md's current-status content (which stage a domain's requirements have reached, how many are SIGNED/DEFERRED at each declared gate stage, how many Conflicts are DECIDED/HELD/UNRESOLVED) MUST be generated from typed carriers ON EVERY `hotam gen-spec` run -- Requirement.gate_signoffs and Conflict lifecycle state, read live via internal/graphfacts's GateSignoffTally/GateFrontier/ConflictLifecycleTally -- NEVER carried as a point-in-time snapshot embedded in a Process's or Step's authored `why` prose. Authored `why` text holds DURABLE rationale only (why a stage exists, why this order, why this process matters) -- a live tally or a dated status claim frozen into that prose inevitably goes stale, because nothing regenerates prose the way `hotam gen-spec` regenerates a projection.",
	Owner:          "framework-author",
	Status:         "DRAFT",
	Why:            "Task #331 (R4-process-why), fourth external review: prat/gpsm-sm's Process.Why literally read \"27 из 32 ФТ... ТЕКУЩЕЕ ПОЛОЖЕНИЕ на 2026-07-21\" while the graph had since moved to 32/32 -- a real, confirmed contradiction between durable authored prose and live graph state, because the prose captured a snapshot nothing ever re-derived. The fix has two enforcement halves. (1) PIPELINE.md now renders a generated \"Live state\" section (internal/generator/pipeline.go's renderPipelineLiveState, called from BuildPipeline) placed BEFORE the first `## Process` section (R-domain-overview-projection's own \"where are we now\" before \"how does this work\" ordering) -- a pure function of graph.json state only (no `today`/date parameter, so gen-spec stays byte-reproducible run to run), rendering one line per gate_stage_order stage (SIGNED/DEFERRED tally via GateSignoffTally, honest \"not started\" beyond the GateFrontier) plus a Conflicts DECIDED/HELD/UNRESOLVED line via ConflictLifecycleTally, omitted entirely when the domain has no declared gate_stage_order and no Conflicts (honest no-op, not an empty placeholder). (2) A narrow, ADVISORY-ONLY lint (internal/invariants/process_why_snapshot.go's check_process_why_snapshot_prose, exported as ProcessWhySnapshotWarnings, wired into cmd/hotam's non-blocking ADVISORY section -- mirrors HonoredSkipWarnings' identical never-registered-in-All shape) flags a Process.Why or Step.Why text that either (a) co-occurs a fixed snapshot-marker phrase (\"текущее положение\" / \"по состоянию на\" / \"as of\" / \"current status\") with an ISO date, or (b) co-occurs a \"N из/of M\" tally with any stage token from the domain's OWN declared gate_stage_order. Deliberately scoped to Process.Why/Step.Why only (never Requirement.Why) and deliberately a small fixed pattern set, not a general number/date heuristic, verified against hotam-spec-self's own 253-requirement graph producing zero false positives. The actual gpsm-sm Process.Why migration (rewriting PR-gpsm-ott-delivery's stale text in the sibling PRAT-hotam repo) is intentionally OUT of this requirement's scope -- a separate follow-up task, not this engine-portion change.",
	Assumptions:    nil,
	Relations:      []ontology.Relation{{Kind: "refines", Target: "R-domain-overview-projection"}},
	Enforcement:    "PROSE",
	EnforcedBy:     nil,
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-23",
	SettledAt:      "",
	SourceRefs:     []string{"internal/generator/pipeline.go", "internal/invariants/authored_prose_snapshot.go", "internal/graphfacts/facts.go", "cmd/hotam/all_violations.go"},
	DeclOrder:      0,
	ImplementedBy:  []string{"internal/generator/pipeline.go:renderPipelineLiveState", "internal/invariants/authored_prose_snapshot.go:checkAuthoredProseSnapshot"},
	VerifiedBy:     []string{"internal/generator/pipeline_livestate_test.go:TestBuildPipeline_LiveStateRendersDedupedGateTallyAndConflictLifecycle", "internal/invariants/authored_prose_snapshot_test.go:TestCheckAuthoredProseSnapshot_FiresOnGpsmSmShapedSentence"},
})

var _ = Requirements.MustRegister("R-requirement-update-signoff-typed", ontology.Requirement{
	ID:             "R-requirement-update-signoff-typed",
	Claim:          "A Requirement UPDATE or Assumption rewrite that records a real human decision MUST carry a typed signoff (ontology.Signoff -- decided_by/date/verbatim/instrument) on the resulting HistoryEntry, whose decided_by resolves to a declared Stakeholder; free-text --decision-ref remains only for mechanical acknowledgments (e.g. a traceability-completion fix binding an existing test to an existing requirement), never a substitute for a typed signoff on a real judgment-call decision.",
	Owner:          "framework-author",
	Status:         "DRAFT",
	Why:            "Task #335 (R4F-req-signoff), fourth external review's final synthesis (§4.4): task #328's landed Requirement UPDATEs (R-shared-projections-mode-independent, R-orientation-faq-answerable) recorded real human approval only as free text in History.Summary -- History.DecidedBy ended up \"\" even though a real human (Marat Karamullin, project owner/resolver) approved with a real verbatim quote («Da, prizemlit oba kak est»), confirmed by direct read of domains/hotam-spec-self/graph.json. Conflict/GateSignoff already REQUIRE typed decided_by/verbatim (task #319); ProposedRequirement/ProposedAssumptionRewrite had no equivalent structure until this task added HistoryEntry.Signoff (internal/ontology/requirement.go), the optional Signoff field on ProposedRequirement/ProposedAssumptionRewrite (internal/proposal/types.go), shape validation (internal/proposal/validate.go's validateHistorySignoffShape -- decided_by/verbatim required, chosen_variant rejected as Conflict-variant-only junk), Stakeholder-resolution at mutate time (internal/proposal/mutate.go's resolveHistorySignoff, deliberately UNGATED unlike ProposedConflict.mutate's zero-Stakeholder escape hatch -- a typed signoff is per-proposal opt-in, so a domain choosing to use it must have named its humans), and two new ongoing invariants (internal/invariants/history_signoff_checks.go): check_history_signoff_has_provenance and check_history_signoff_decided_by_is_known_stakeholder. cmd/hotam/semantic_gate.go's hasOverride now accepts a typed Requirement Signoff on par with --ack-conflict/--decision-ref for overriding the semantic-conflict gate. This requirement is DRAFT, not applied, pending resolver review -- per R-ai-presents-not-decides/R-decided-needs-human-signoff, adopting a NEW authoring obligation (not just a NEW capability) is a judgment call for the resolver, not something the same task that built the capability should also unilaterally mandate. NOTE (surfaced, not resolved, by this same task): hotam-spec-self's own domain declares only ai-agent/domain-user/framework-author/framework-reviewer Stakeholders -- there is NO Stakeholder for Marat Karamullin in THIS domain (unlike PRAT-hotam's gpsm-sm domain, which does have one) -- so the first REAL use of this feature in hotam-spec-self itself needs either a new ProposedStakeholder or a resolver decision that domain-user/framework-author is the honest id to use.",
	Assumptions:    nil,
	Relations:      []ontology.Relation{{Kind: "refines", Target: "R-signoff-preserved-in-substrate"}, {Kind: "depends_on", Target: "R-decided-needs-human-signoff"}},
	Enforcement:    "PROSE",
	EnforcedBy:     nil,
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-23",
	SettledAt:      "",
	SourceRefs:     []string{"internal/ontology/requirement.go", "internal/ontology/signoff.go", "internal/proposal/types.go", "internal/proposal/validate.go", "internal/proposal/mutate.go", "internal/invariants/history_signoff_checks.go", "cmd/hotam/semantic_gate.go"},
	DeclOrder:      0,
	ImplementedBy:  []string{"internal/proposal/mutate.go:resolveHistorySignoff", "internal/proposal/validate.go:validateHistorySignoffShape"},
	VerifiedBy:     []string{"internal/proposal/history_signoff_test.go:TestApply_Requirement_UpdateWithSignoff_LandsIntoHistory", "internal/invariants/history_signoff_checks_test.go:TestHistorySignoffChecks_CleanOnRealGraph"},
})

var _ = Requirements.MustRegister("R-resolver-distinct-from-owners", ontology.Requirement{
	ID:             "R-resolver-distinct-from-owners",
	Claim:          "Every Conflict's resolver shall be a Stakeholder who is NOT the owner of any of the conflict's members.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "A tension lives BETWEEN stakeholders; if the resolver owned a side, the tension would be judged by an interested party and quietly resolved in their favor. The structural twin of R-ai-presents-not-decides.",
	Assumptions:    []string{"A-stakeholders-care"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_resolver_not_a_member_owner"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-06-30",
	SettledAt:      "2026-06-30",
	SourceRefs:     []string{},
	DeclOrder:      6,
})

var _ = Requirements.MustRegister("R-shared-projections-mode-independent", ontology.Requirement{
	ID:             "R-shared-projections-mode-independent",
	Claim:          "Every docs/gen/ projection generated by plain `hotam gen-spec` (implemented_by TRACEABILITY.md/COVERAGE.md/REPO-MAP.md's own Build* functions) MUST render byte-identically regardless of whether the SAME `gen-spec` invocation also passed `--spec`, EXCEPT SPEC.md itself, which ALWAYS remains the sole `--spec`-shaped artifact whose staleness is separately enforced by check_spec_md_current.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "task #334 (R4F-bind-test, fourth external review §4.3): the requirement landed SETTLED/PROSE with empty implemented_by/verified_by despite a real regression test for exactly this property already existing and being named in the requirement's own claim evidence — TestGenSpec_SharedProjectionsModeIndependent (cmd/hotam/gen_spec_test.go). This claim is about GENERATOR/engine behavior (a property of cmd/hotam/gen_spec.go and internal/generator/traceability.go+coverage.go), the same shape as the neighboring R-empty-content-gen-notice, which binds via enforced_by to a bare Test* name rather than the authored implemented_by+verified_by path (that path is reserved for path-qualified references into a domain's own authored spec/ tree, per internal/invariants/authored_links.go and PLAN-authored-spec-discipline.md §5/§12's disjunctive gate — an engine-self-hosting requirement is not forced to fabricate one). Binding enforced_by alone satisfies check_enforced_requires_enforcer_or_authored_link's disjunction and check_enforced_by_resolvable resolves TestGenSpec_SharedProjectionsModeIndependent via the repo-wide cmd/**/*_test.go scan. Mechanical traceability-completion fix, not a new judgment call — decision-ref: task #334, review-sourced finding, linking an existing test to an existing requirement.",
	Assumptions:    nil,
	Relations:      []ontology.Relation{{Kind: "refines", Target: "R-authored-spec-projections-are-derived"}, {Kind: "depends_on", Target: "R-drift-structurally-impossible"}},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"TestGenSpec_SharedProjectionsModeIndependent"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-23",
	SettledAt:      "2026-07-23",
	SourceRefs:     []string{"cmd/hotam/gen_spec.go", "internal/generator/traceability.go", "internal/generator/coverage.go", "cmd/hotam/gen_spec_test.go"},
	DeclOrder:      0,
})

var _ = Requirements.MustRegister("R-signoff-preserved-in-substrate", ontology.Requirement{
	ID:             "R-signoff-preserved-in-substrate",
	Claim:          "A resolver signoff on a DECIDED/HELD Conflict or a transitioned Assumption shall be preserved as a Signoff payload IN the graph node (not only in gitignored proposal JSON) -- decided_by, date, verbatim (optional), instrument and chosen_variant (for HELD->DECIDED) are auditable from the substrate.",
	Owner:          "framework-author",
	Status:         "SETTLED",
	Why:            "K2(a) from lens-1-ontology review: AssumptionTransition's decided_by was required at validation but the writer wrote only status+reason to the graph, so the signature evaporated into gitignored JSON -- R-trust-anchor-mechanism was not auditable from the substrate. K2(b): HELD->DECIDED erased Variants (the non-chosen implies/costs -- anti-relitigation cargo -- were overwritten); the chosen variant lived only as prose inside DECIDED(...). The Signoff payload is ontology.Signoff (internal/ontology/signoff.go): a struct (NOT a new node type, anti-RDF -- mirrors Variant's rationale) carrying DecidedBy + Date + Verbatim + Instrument + ChosenVariant, attached as *Signoff on Conflict.Signoff and Assumption.Signoff (internal/ontology/conflict.go, internal/ontology/assumption.go). The proposal writer builds it: ProposedConflictTransition.mutate (internal/proposal/mutate.go) sets c.Signoff = &ontology.Signoff{...} when the transition is a decision with a non-empty DecidedBy (Instrument defaults to SignoffInstrumentPersonal), and ProposedAssumptionTransition.mutate does the equivalent for assumptions; variants are preserved across HELD->DECIDED. Instrument (SignoffInstrumentPersonal / SignoffInstrumentDEL, internal/ontology/signoff.go) is an explicit seam for the future verifiable-signature wave (R-decided-by-verifiable-signature is OPEN -- this atom does NOT bind to git/crypto, only names the seam). check_signoff_chosen_variant_resolves (internal/invariants/typed_anchors.go) enforces a non-empty chosen_variant is the id of one of the conflict's variants; check_decided_conflict_carries_signoff enforces consistency (signoff.decided_by == conflict.decided_by) when a signoff is present, SOFT (pre-existing decisions without signoff are legitimate, no migration forcing). Task #335 (R4F-req-signoff, fourth external review) EXTENDED this requirement's enforced_by with two more checks -- check_history_signoff_has_provenance and check_history_signoff_decided_by_is_known_stakeholder (internal/invariants/history_signoff_checks.go) -- mirroring the EXACT precedent task #319 set on the sibling anchor R-gate-signoff-single-carrier (extend an existing Signoff-topic anchor's enforced_by rather than create a new standalone-claim requirement just to satisfy check_bijection_r_to_enforcer). These two checks generalize this requirement's own \"a signoff is auditable from the substrate\" claim to the shared ontology.HistoryEntry.Signoff field (Requirement/Assumption/Axis/EntityType/Process History) that ProposedRequirement/ProposedAssumptionRewrite can now optionally carry on an UPDATE (task #335): any HistoryEntry with a non-nil Signoff MUST carry non-empty decided_by/verbatim, and decided_by, when present, MUST resolve to a known Stakeholder -- the same auditability guarantee this requirement already makes for Conflict.Signoff/Assumption.Signoff, extended to the newer HistoryEntry.Signoff carrier. This domain (hotam-spec-self) currently has no landed HistoryEntry carrying a signoff (task #328's R-shared-projections-mode-independent/R-orientation-faq-answerable entries record real human decisions as free-text History.Summary with DecidedBy==\"\", the exact gap this task's new typed-signoff capability exists to close for FUTURE UPDATEs -- neither check retroactively flags those two entries, since Signoff is nil on both), so both new checks are honest zero-violation passes here today -- this UPDATE exists only to anchor them for the bijection discipline (R-bijection-r-to-enforcer), not to claim hotam-spec-self has adopted the new authoring obligation (that adoption decision is deliberately left DRAFT, unlanded, in proposals/task335-r4f-req-signoff/001-R-requirement-update-signoff-typed.json, pending resolver review).",
	Assumptions:    []string{"A-stakeholders-care", "A-bootstrap-self-applies"},
	Relations:      []ontology.Relation{},
	Enforcement:    "ENFORCED",
	EnforcedBy:     []string{"check_signoff_chosen_variant_resolves", "check_decided_conflict_carries_signoff", "check_history_signoff_has_provenance", "check_history_signoff_decided_by_is_known_stakeholder"},
	MTag:           "",
	Enforceability: "ENFORCEABLE",
	Summary:        "",
	CreatedAt:      "2026-07-06",
	SettledAt:      "2026-07-12",
	SourceRefs:     []string{"internal/ontology/signoff.go", "internal/ontology/conflict.go", "internal/ontology/requirement.go", "internal/proposal/mutate.go", "internal/proposal/validate.go", "internal/invariants/typed_anchors.go", "internal/invariants/history_signoff_checks.go"},
	DeclOrder:      268,
})
