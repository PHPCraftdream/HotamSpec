<!-- AUTOGENERATED from spec/src/hotam_spec + spec/content — do not edit by hand. Edits: docstrings/content -> python tools/gen_spec.py -->
reader: (unresolved-reader)

# Entities

> Generated by `spec/tools/gen_spec.py` from the active domain's `graph.py:build_graph()`. Do not hand-edit.

## customer

A paying account that may be ACTIVE, SUSPENDED, or CLOSED.

### Lifecycle

```mermaid
stateDiagram-v2
    [*] --> ACTIVE
    ACTIVE --> SUSPENDED : suspend
    ACTIVE --> CLOSED : close
    SUSPENDED --> ACTIVE : reopen
    ACTIVE: ACTIVE (initial)
    SUSPENDED: SUSPENDED (quiescent)
    CLOSED: CLOSED (quiescent)
```

- States: `ACTIVE` (initial), `SUSPENDED` (quiescent), `CLOSED` (quiescent)
- Transitions: `suspend`, `close`, `reopen`
- Cyclic: false

### Fields

| name | kind | required | ref_target |
|------|------|----------|------------|
| email | string | true |  |
| tier | enum | false |  |
| owner | reference | true | stakeholder |

### Covered by

- `check_entity_type_lifecycle_wellformed`
- `check_entity_instance_state_in_lifecycle`
- `check_entity_instance_required_fields`
- `check_entity_instance_id_prefix`
- `check_entity_instance_refs_resolve`
- `check_entity_field_kind_known`
- `check_typed_anchors_entity`

### Instances

| id | state | email | tier | owner |
| --- | ----- | ----- | ---- | ----- |
| ENT-customer-acme | ACTIVE | billing@acme.com | gold | finance |

## invoice

A billing document owed by a customer, DRAFT through PAID/VOID.

### Lifecycle

```mermaid
stateDiagram-v2
    [*] --> DRAFT
    DRAFT --> SENT : send
    SENT --> PAID : pay
    SENT --> VOID : void
    DRAFT: DRAFT (initial)
    SENT: SENT (normal)
    PAID: PAID (quiescent)
    VOID: VOID (quiescent)
```

- States: `DRAFT` (initial), `SENT` (normal), `PAID` (quiescent), `VOID` (quiescent)
- Transitions: `send`, `pay`, `void`
- Cyclic: false

### Fields

| name | kind | required | ref_target |
|------|------|----------|------------|
| amount_cents | number | true |  |
| customer | reference | true | customer |

### Covered by

- `check_entity_type_lifecycle_wellformed`
- `check_entity_instance_state_in_lifecycle`
- `check_entity_instance_required_fields`
- `check_entity_instance_id_prefix`
- `check_entity_instance_refs_resolve`
- `check_entity_field_kind_known`
- `check_typed_anchors_entity`

### Instances

| id | state | amount_cents | customer |
| --- | ----- | ------------ | -------- |
| ENT-invoice-acme-001 | SENT | 12500 | ENT-customer-acme |

## Entity-state tensions

- `PR-auto-suspend-fraud` × `PR-billing-close-delinquent` — both drive entity 'customer' but to disjoint resting states: ['SUSPENDED'] vs ['CLOSED'] — likely conflict on axis behavioral-customer-state
