# Checkpoint — 2026-07-10 16:32

## Session summary

Продолжение марафона Hotam-Spec из предыдущей сессии (сама сессия началась с `/resume 2026-07-10-0300.md`). В этой части: (1) закрыт коммит #92 (apply_proposal-дедупликация) и #93 (позиционирование J1 — переформулировка тэглайна под «память и дисциплина для человек+LLM-флота», Conflict = свойство, не заголовок); (2) исполнена и закоммичена вся цепочка Этапов A-F синтез-плана 2026-07-09/10 (задачи #103-108: гигиена кода, python-first свип, граф-мутации A-ontology-transfers+2 enforcement-апгрейда, консолидация инструментов, дисциплина флота/attention-источники, batch-perf-фикс) — финальное `fh`-ревью GREEN; (3) пять параллельных `fxx`-агентов провели повторное пятилинзовое ревью (redundancy/simplification/usability/applicability/quality), синтез записан и заведено 6 задач (#103-108, уже закрыты выше — нумерация переиспользована для новой волны); (4) отдельный `fxx`-агент исследовал производительность (gen_spec/tests/LAND-цикл) — отчёт `docs/reviews/2026-07-10-perf-investigation.md`; (5) стьюард передал ВНЕШНЕЕ продуктовое ревью (не от агента этой сессии) — координатор провёл полный CONFRONT-разбор (проверил КАЖДЫЙ факт против репозитория построчно, все подтвердились), классифицировал находки A(новое)/B(уже сделано)/C(конфликт с прошлыми решениями)/D(неточности), записал план `docs/reviews/2026-07-10-external-review-plan.md` с 6 этапами G-L; (6) `/babygoal 20m` со стратегией "агенты @o4l" исполнил Этапы G-K последовательно (#109-113): wheel-упаковка tools/ (build-time copy в gitignored `_tools/`), consumer write-path с атомарным rollback (+ попутный критичный фикс — все 26 CLI-команд молча возвращали exit 0 из-за отсутствия `sys.exit(rc)` в `_dispatch.py`), stale-refs sweep + хвосты ревью, новый CLI `hotam-req` (list/show/search/patch/context), perf-фиксы (suite 72.8s→54.7s, gen_spec 5.8s→4.1s).

**Реально наблюдаемый инцидент**: во время исполнения Этапа H координатор ЛИЧНО зафиксировал живую порчу корневого CLAUDE.md — pin-файл `domains/.active-domain` кратковременно перезаписался на несуществующий `my-shop` (утечка из editable-e2e теста в реальный чекаут фреймворка). Самовосстановился к следующему тику; агент Этапа H нашёл и исправил root cause (backup/restore в finally-блоке теста). Независимое `oh`-ревью подтвердило фикс GREEN.

**В МОМЕНТ ЧЕКПОИНТА**: фоновый агент `fh` (agentId `a179dd6777749fe0a`) выполняет финальное сквозное ревью всей цепочки Этапов G-K (5 коммитов, #109-113) — **ещё не завершён**, результат не получен. Задача #114 (Этап L — 6 архитектурных вопросов стьюарду: per-node JSON хранилище vs R-rdf-store, ядро/плагины, freshness-дизайн, uv_build, INHERENTLY_PROSE, эскалация D3/E1) — pending, намеренно НЕ начата (разговор со стьюардом, не делегируется, не запускается автоматически через babysit-тик).

**Пульс**: `[P4] OPEN_ITEM R-decided-by-verifiable-signature` (входит в перечень Этапа L). T2 на последний известный момент: 1306 passed, 4 skipped, ~56-80s.

## Active goal

Нет активного `/goal` Stop-hook в этой сессии (использовался `/babygoal` — TaskList-driven, не `/goal`-driven).

## TaskList

### pending
- #114 Этап L — [PRESENT] Триаж-вопросы стьюарду (6 вопросов, разговор) — не заблокирована, но намеренно не стартуется автоматически

### recently completed
- #113 Этап K — Perf top-5 (коммит 5c51bc1)
- #112 Этап J — CLI UX: hotam-req (коммит 2d97002)
- #111 Этап I — Stale-refs sweep + хвосты ревью (коммит ca45f36)
- #110 Этап H — Consumer write-path + rollback (коммит 5a4f9a8, oh-ревью GREEN)
- #109 Этап G — Wheel-упаковка (коммит b47453d)
- #103-108 — вся цепочка Этапов A-F синтез-плана (fh-ревью GREEN)

## Decisions

- Этап K, п.2 (--batch tier-selection бага): выбран вариант «всегда T2 для --batch» вместо per-item creation-tracking — проще, устраняет весь класс бага, экономия T1 в редком batch-пути незначима.
- Этап G: build-time копирование spec/tools/ в gitignored `hotam_spec/_tools/` (не переписывание build-backend `uv_build` — это отдельный вопрос Этапа L).
- Этап H: rollback реализован в ОБЕИХ ветках (self-hosting pytest-путь И consumer in-process путь) — конфликта с R-verify-closure-per-action/R-land-tier-trace-* не найдено, честная остановка не понадобилась.
- Найденный вживую баг утечки `domains/my-shop`/`.active-domain` из editable-e2e — исправлен в рамках самого Этапа H (не отложен), подтверждён независимым oh-ревью.
- Тема пилотов на реальных внешних системах (из внешнего ревью, п. P3-10) сознательно исключена из вопросов Этапа L по явному слову стьюарда «сам решу когда пора».

## Open questions

- Результат fh-ревью Этапов G-K (agentId `a179dd6777749fe0a`) — не получен на момент чекпоинта. При возобновлении: дождаться уведомления.
- 6 вопросов Этапа L (#114) — не заданы стьюарду: (1) per-node JSON хранилище vs R-rdf-store конфликт, (2) ядро/плагины (D1-рескоуп), (3) freshness-дизайн (created_at/settled_at/last_reviewed_at), (4) build-backend uv_build → нейтральный, (5) 2 PROSE-правила → INHERENTLY_PROSE, (6) эскалация D3/E1.
- Пуш — накопилось 5 незапушенных коммитов (b47453d..5c51bc1) плюс более ранние из этой марафонской сессии, ждёт явного слова стьюарда.

## Repo state

```
(рабочее дерево чистое)
```
```
5c51bc1 perf: Этап K -- gen_spec/suite/LAND speedups from perf-investigation (#113)
2d97002 feat: Этап J -- hotam-req CLI (list/show/search/patch/context) (#112)
ca45f36 docs+fix: Этап I -- stale spec/content refs + fh/oh review tails (#111)
5a4f9a8 fix: Этап H -- consumer write-path CRUD e2e + atomic rollback (#110)
b47453d feat: Этап G -- wheel packaging + external-review CONFRONT plan (#109)
```

Незапушенных коммитов относительно `origin/master`: 5 (только в этом диапазоне; фактически больше с учётом более ранних этапов сессии).

Активный babysit-cron: `054715e4`, каждые 20 минут (`7,27,47 * * * *`), session-only.
