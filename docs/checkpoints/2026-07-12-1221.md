# Checkpoint — 2026-07-12 12:21 [review-score-uplift-wave]

## Session summary

Продолжение долгой сессии по Go-порту HotamSpec (D:\ai_dev\prat\HotamSpec). Ранее в этой же сессии: восстановили не-кодовый контент, потерянный при замене Python→Go (коммиты `1a95f53`, `bd04281`), добавили зеркалирование CLAUDE.md в AGENTS.md/GEMINI.md, затем добавили параллелизм в gen-spec и тесты (коммит `4512d60`, последний в истории на момент этого чекпоинта). Далее пользователь принёс внешнее ревью Go-порта с оценками по 8 областям (2–8/10, средняя 5/10) и попросил поднять все оценки до 9/10. Через `/babygoal` (агенты `@sm`, интервал 15м, `/babysit`-крон уже снят по завершении) декомпозировали план на 18 задач (`#8`–`#26`: P0×6, P1×7, P2×5) плюс финальную верификацию (`#26`) и независимое ревью (`#27`, агент `@om`). Все 18+2 задачи выполнены волнами по 2–4 параллельных агента (с явным разведением файловых пересечений через `blockedBy`, особенно вокруг `cmd/hotam/main.go` switch и `internal/generator/testdata`). Я лично перепроверял `build/vet/test/-race` после КАЖДОЙ волны и нашёл/починил минимум 3 реальные регрессии, которые агенты-исполнители пропустили: (1) захардкоженные `<`/`>` в поле `Command` инструментов ломали запись файлов на Windows; (2) `cmd/hotam/main_test.go` ссылался на golden-файлы `internal/generator/testdata/*.md`, которые другая задача удалила при переходе на компактную синтетическую фикстуру; (3) `.github/PULL_REQUEST_TEMPLATE.md` и `docs/playbooks/P4-OPEN-ITEM.md` всё ещё давали Python-инструкции — ни одна волновая задача их не затронула. Финальный python-грep, ручной сквозной агентский сценарий (`what-now→req show→due→inspect→land`) и повторный gen-spec (детерминизм подтверждён чек-суммами) — всё зелёное. Независимое ревью `@om` (тоже не поверил отчётам, сам гонял команды) подтвердило: все 6 P0 закрыты по факту (проверено живым round-trip snake_case JSON и сдвигом даты для freshness), реальных регрессий не нашло, но выставило честные оценки ниже 9/10 — Итоговое соответствие цели 5→7, Актуальность требований 2→4 (сам корпус из 275 требований всё ещё описывает Python-систему в текстах claim), плюс менее серьёзные находки: `hotam inspect` даёt 1388 кандидатов с ~80% шумом (score≤4), `GraphIndex` (задача P2-4) внедрён только в 2 местах из заявленных (`internal/diagnose`, `internal/query`), `internal/invariants` всё ещё сканирует линейно (7 мест) — я лично перепроверил оба этих факта grep'ом, подтвердились. Также подтверждено: CLAUDE.md/AGENTS.md/GEMINI.md — по 22464 байта каждый, не дотянули до целевых <15КБ (задача P2-1 частично); `go.mod` module path (`github.com/PHPCraftdream/HotamSpecGo`) расходится с реальным git remote (`github.com/PHPCraftdream/HotamSpec.git`) — осознанно оставлено как TODO, не переименовано самовольно. **Рабочее дерево сейчас содержит некоммиченную волну ~301 путь (41 новый, ~260 изменённых) — коммит НЕ сделан**, так как в этой волне не было явной просьбы «закоммить», только «реализуй и запусти ревью». TaskList после завершения волны сейчас пуст (все 20 задач `#8`–`#27` были completed и, похоже, больше не возвращаются инструментом TaskList — это не потеря работы, просто список опустел).

## Active goal
none — Stop-хук с условием этой волны («реализуй таски по поднятию оценок...») должен был автоматически сняться по выполнении; отдельного `/goal` в сессии не заводили.

## TaskList
TaskList сейчас пуст (`TaskList` вернул "No tasks found"). Последнее известное состояние — все 20 задач волны были `completed`:
### recently completed (последние известные, по памяти сессии — не из живого TaskList)
- #8 P0-1: json-теги + строгий декодер для Proposed*
- #9 P0-2: чистка Python-эры из internal/generator
- #10 P0-3: чистка Python-эры из internal/methodology
- #11 P0-4: README.md/CONTRIBUTING.md под Go
- #12 P0-5: синхронизация docs/PROPOSAL-REFERENCE.md, QUICKSTART, docs/demo
- #13 P0-6: перегенерация CLAUDE.md/AGENTS.md/GEMINI.md + docs/gen
- #14 P1-1: hotam req show/list/search/context/related
- #15 P1-2: hotam due + freshness-движок
- #16 P1-3: backfill review-метаданных 275 требований
- #17 P1-4: hotam land (единый pipeline)
- #18 P1-5: hotam inspect (advisory-конфликты)
- #19 P1-6: honest tools-registry (Status Ported/Declared)
- #20 P1-7: hotam init + внешний e2e-тест
- #21 P2-1: компактный AGENT-CONTEXT.md (CLAUDE.md сжат частично, не до цели)
- #22 P2-2: golden-фикстуры 734К→116К (синтетический fixture-graph.json)
- #23 P2-3: слияние module_docs_data.go в methodology (единый источник)
- #24 P2-4: GraphIndex (внедрён ЧАСТИЧНО — см. открытые вопросы)
- #25 P2-5: go install, hotam version, README Install-секция
- #26 FINAL: сквозная верификация волны (build/vet/test/race/shuffle/python-gate/e2e — всё зелёное, план коммита готов, коммит не сделан)
- #27 Ревью om (завершено, оценки и находки — см. summary выше)

## Decisions
- Раскладывать 18 задач волны на волны по 2–4 параллельных `@sm`-агента, разводя файловые пересечения через `blockedBy` (особенно `cmd/hotam/main.go`, `internal/generator/testdata`), а не пытаться распараллелить всё сразу.
- Регрессии, найденные при верификации волн (Windows-несовместимые имена файлов, устаревшие golden-ссылки, Python-инструкции в PR-шаблоне/playbook), чинил сам напрямую (мелкие, механические, хорошо понятные фиксы), а не через новых агентов — экономия контекста.
- `#26` (финальная верификация) и часть её работы выполнил сам, а не через нового `@sh`/`@sm`-агента — уже имел весь контекст волны, не было смысла пересобирать его заново для нового агента.
- Ревью `@om` запустил строго после `#26` (`blockedBy`), с явным требованием не доверять отчётам агентов-исполнителей и самому гонять команды — это сработало, om нашёл 2 находки, которые я потом сам перепроверил и подтвердил.
- Коммит НЕ сделал несмотря на завершённую волну — правило проекта требует явной просьбы на коммит, а в этой волне такой просьбы не было (только "реализуй и запусти ревью").

## Open questions
- Коммитить ли текущую волну (~301 путь)? Ждёт явного слова пользователя.
- Как поступить с находками om — заводить ли follow-up задачи: (1) пересмотр текста самих 275 требований под Go-реальность (Актуальность требований застряла на 4/10 именно из-за этого); (2) фильтрация шума `hotam inspect` (score-порог, сейчас 1388 кандидатов, ~80% шум); (3) довнедрение `GraphIndex` в `internal/invariants` (7 линейных сканов) и в `internal/proposal/mutate.go`/`internal/query/show.go` (используют другой, старый O(n) механизм `ontology.RequirementByID`); (4) дальнейшее сжатие CLAUDE.md-триады до <15КБ; (5) решение по расхождению `go.mod` module path vs git remote.
- Пользователь не подтверждал и не отклонял ни один из этих follow-up пунктов — решение за ним.

## Repo state
```
301 путь изменён/добавлен (некоммичено), сводка не хранится дословно в чекпоинте — см. `git status --short` в репозитории для точного списка
```
```
4512d60 perf: parallelize gen-spec file writes and enable t.Parallel() across the test suite
4b80ae4 docs: session checkpoint 2026-07-12-1008
bd04281 feat: mirror root CLAUDE.md into AGENTS.md and GEMINI.md
1a95f53 restore: recover non-code assets + complete Go port (hotam-dev domain, CLAUDE.md generation)
c3472d0 rewrite: replace Python implementation with Go port
```

Branches: `master` (HEAD `4512d60`, up to date with `origin/master` — коммит `4512d60` уже был запушен в прошлой части сессии до старта этой волны, все более новые правки волны `#8`–`#27` НЕ запушены и не закоммичены), `backup/python-legacy-2026-07-12` (Python-эра снапшот, запушен ранее).
