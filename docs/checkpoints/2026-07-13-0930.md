# Checkpoint — 2026-07-13 09:30 [external-review-response-wave]

## Session summary

This session responded to a second external review (score 8.2/10) of the HotamSpec Go implementation (`D:\ai_dev\prat\HotamSpec`, git branch `master`, remote `PHPCraftdream/HotamSpec`), continuing directly from an earlier segment that had already: renamed `Ported`/`Declared` tool statuses to `Implemented`/`Planned`, translated README + methodology atoms to English, and stripped "Go port of Python" framing from source comments and graph content across both domains (`domains/hotam-spec-self`, `domains/hotam-dev`).

The user asked to study the review, create tasks, and implement them via `/crush` sub-agents (later switched mid-session to the `sh` Claude sub-agent when a `/crush` provider hit a peak-hours block). All P0/P1/P2 items from the review's own priority list were decomposed into TaskList items (#75–#95) and executed one at a time, each independently re-verified by the orchestrator (never trusting a sub-agent's self-report) via `go build`, `go vet`, `gofmt -l`, `go test -count=1 ./...`, and `go run ./cmd/hotam all-violations --domain <domain>` after every single change.

Major completed work: (1) the 91-item "SETTLED but not ENFORCED" enforcement-debt backlog was fully triaged — 7 items reclassified `ENFORCEABLE→INHERENTLY_PROSE` (category B), 42 items got real new Go tests wiring them to `ENFORCED` across 7 sequential sub-batches A1–A7 (a new `internal/selfcheck` package was created for repo-hygiene static scans), bringing closeable debt from 91 down to 42 (= the 33 legitimately feature-blocked C1 items + 9 C2 items needing a resolver decision); (2) `ProposedReviewMark` now requires non-empty `evidence` (closes the "date stamped vs. content re-reviewed" ambiguity the review flagged — verified via an `fm` design consultation first, to confirm this doesn't relitigate the already-REJECTED `R-boot-cite-measured` form-metric-theatre pattern); (3) `hotam land` is now transactional — snapshots `graph.json`+`graph.lock` before applying, rolls back + re-runs `gen-spec` if a later stage fails, so a failed land can never leave a new graph.json next to stale docs; (4) `inspect`/`confront`'s lexical-overlap noise was cut 1231→234 candidates (−81%) via a corpus-frequency dynamic stop-word layer (data-driven, not a hardcoded domain word list, to respect the content-free-framework-source constraint); (5) a new `hotam status --json` command gives a one-shot pulse (top action, debt, freshness, violations, node count), cross-verified against independently-run `what-now`/`due`/`all-violations`; (6) JSON `null`→`[]` fixed across every `--json`-emitting command; (7) `internal/invariants` was profiled for a possible `GraphIndex` migration — investigation concluded NO migration was warranted (79% of `all-violations`' CPU time is `gate.TestFuncNames()`'s filesystem/AST scan, not graph-collection lookups; every check already precomputes O(1) lookup maps) — reported honestly as a no-op outcome rather than forcing a change; (8) an "which docs/gen files are boot-critical vs reference vs archival" index was added to the generator source for `AGENT-CONTEXT.md` (low-risk, documentation-only — `docs/gen/graph.json`, initially suspected as bloat, turned out to be requirement-mandated by `R-drift-structurally-impossible`, so it was kept); (9) the `settled_at` corruption bug (found and fixed earlier this session in `internal/proposal/mutate.go`) was git-forensically diffed against the last real commit (`9af0176`) — only 83 requirements (not the originally-estimated 185) were actually corrupted by this session's specific bug and were restored; a separate 185-item cluster dated `2026-07-12` turned out to already exist at HEAD (i.e., predates this session, likely a legitimate historical bulk-settlement event, out of scope).

Two items were deliberately NOT auto-resolved because they require the human resolver's judgment, per this project's own `R-ai-presents-not-decides` rule: a 9-item "category C2" enforcement-debt list (each with 2–3 options, e.g. whether to rename the Go module path to kebab-case — flagged as the highest-risk option in the whole batch, touching 119 files/178 import occurrences and reversing a deliberate recent commit) was written up as a fully re-verified decision brief at `docs/reviews/2026-07-13-category-c2-decision-brief.md` (re-checked against current graph/code state, correcting 4 of the original 9 framings from an earlier `fm` consultation) and relayed to the user with a recommendation table — awaiting their picks, not yet acted on. The user also explicitly asked to delete the "release tag v0.1.0" task (it required explicit push approval and was blocked on other now-completed work) — done.

All work this entire session (both this segment and the prior one) remains **UNCOMMITTED** — `git status --short` shows 181 modified/new paths against the last real commit `9af0176`. Per the user's standing global instructions, no commit/push has been made and none should be made without an explicit separate request.

The `/babysit` cron (armed early this session for the `/babygoal`-driven wave) self-deleted once the TaskList emptied — there is currently NO active cron/heartbeat. A one-shot peak-hours-retry wakeup that had been scheduled for `hotam land`'s original `/crush` attempt was cancelled once the user redirected that task to the `sh` sub-agent instead (crush's peak-hours block no longer applies since crush isn't in use for it anymore).

## Active goal

None — no `/goal` Stop hook is in force this session.

## TaskList

Empty. All decomposed tasks (#65–#95) reached `completed` or `deleted` (task #79, "release tag v0.1.0", was explicitly deleted by the user this segment). The babysit cron self-deleted per its own "TaskList empty" stop condition.

### recently completed (last 10, chronological)
- #84 Восстановить settled_at из git-истории (83 of the originally-estimated 185 actually needed restoration; rest predates this session)
- #80 hotam status/pulse — единая сводка для агента одной командой
- #81 JSON-вывод — пустые массивы вместо null
- #82 GraphIndex во всех query-операциях + оценка invariants (investigated, no migration warranted — documented why)
- #83 Разделить agent-context доки и исторический архив (doc-index added, no physical reorg — low risk)
- #78 stop-words + улучшение порогов в inspect/confront (1231→234 candidates)
- #77 P1: сделать land транзакционным (graph + docs атомарно)
- #87 Category C2 enforcement debt: presented (not decided) — decision brief written
- #76 P0: freshness semantics — evidence-поля при review-marks
- #75/#85/#88–#95 P0 enforcement debt (91→42, categories A+B fully closed)

### deleted
- #79 P1: выпустить тег v0.1.0 (user explicitly deleted — was blocked on #77 + needs explicit push approval)

## Decisions

- Chose corpus-frequency-based dynamic stop-word exclusion over a hardcoded extra word list for `inspect`/`confront` noise, specifically to avoid violating the framework's own content-free-source constraint (a fixed list of "requirement"/"enforce"/"graph" baked into `internal/diagnose` would itself be exactly the class of violation `internal/selfcheck` was built to catch).
- Chose a compensating-rollback design for transactional `land` (snapshot + restore + re-run `genSpec`, since `genSpec` is a pure function of on-disk `graph.json`) over a full pre-commit staging-directory swap, judged lower-risk on Windows and sufficient for the actual failure mode the review flagged.
- Declined to migrate `internal/invariants` to `GraphIndex` after profiling showed the real bottleneck is filesystem/AST-scanning in `gate.TestFuncNames()`, not graph-collection lookups — reported as a legitimate no-op finding rather than forcing unnecessary code churn.
- Declined to physically reorganize `docs/gen/` (option B in task #83) after confirming every file has a real consumer (including the initially-suspected `docs/gen/graph.json`, which is requirement-mandated) — chose the lower-risk documentation-only fix (option A).
- Explicitly did NOT resolve the 9 category-C2 items or rename the Go module path — both require the resolver's judgment call, especially the module-rename option which reverses a deliberate prior commit (`4325ac8`) and touches 119 files.
- Switched task #77's execution mechanism mid-flight from `/crush` to the `sh` Claude sub-agent per explicit user instruction, after `/crush`'s provider hit a peak-hours block — preserved the already-completed partial work rather than restarting.

## Open questions

- The 9 category-C2 enforcement-debt items are awaiting the user's picks — full brief at `docs/reviews/2026-07-13-category-c2-decision-brief.md`, summary table relayed in-conversation. None have been acted on.
- Whether/when to commit and push this entire uncommitted wave (181 changed paths) — not requested yet, standing instruction is never to do so without explicit ask.
- The pre-session-origin `2026-07-12`-dated cluster of 185 `settled_at` values (confirmed already present at commit `9af0176`, i.e., not this session's bug) was flagged but not further investigated — whether it reflects a genuine historical bulk-settlement event or an even-older undetected corruption is unresolved and was judged out of scope for task #84.

## Repo state

```
 M AGENTS.md
 M CLAUDE.md
 M CONTRIBUTING.md
 M GEMINI.md
 M README.md
 M cmd/hotam/apply_proposal_json_test.go
 M cmd/hotam/due.go
 M cmd/hotam/due_test.go
 M cmd/hotam/gen_spec.go
 M cmd/hotam/land.go
 M cmd/hotam/land_test.go
 M cmd/hotam/main.go
 M cmd/hotam/main_test.go
 M cmd/hotam/tool_wiring.go
 M cmd/hotam/tool_wiring_test.go
 ... (181 total changed/new paths — mostly domains/*/docs/gen/* regenerated output,
      internal/* source + tests, and proposals/wave5-deportify/, wave6-enforcement-debt/*
      JSON proposal audit trails)
```

```
9af0176 docs: final independent review post score-uplift wave
6918865 feat: fuller hotam version + cross-platform release CI (P2-6)
4325ac8 chore: rename Go module to match the real git remote (P1-6)
a38cb2c chore: gofmt whole tree, add CI workflow and Makefile (P1-5)
d130c62 test: raise loader/generator/paths coverage to 86-92% (P2-4)
```
