# Checkpoint — 2026-07-08 00:00

## Session summary

Марафон продолжился с прошлой сессии (см. предыдущие чекпоинты). Основная арка: (1) шестилинзовое ревью фреймворка (6 fxx-агентов → `docs/reviews/2026-07-05-six-lens-review.md` + `lens-1..6-*.md`), (2) реализация плана из ревью волнами с независимым ревью+коммитом на каждую, (3) интерактивная подпись-сессия со стьюардом (по одному вопросу, просто) закрывшая почти весь накопленный долг.

**Ключевой сдвиг сессии — суверенность хоста.** Стьюард поймал: `setup_context_hook.py --patch-global` патчил глобальный `~/.claude/cah-bin/bin/cah-status.js` — грубое нарушение. Откачено, весь механизм измерения контекста через хост СНЕСЁН (context.py честно UNMEASURED навсегда), заведено правило `R-work-within-launch-dir` («оператор не трогает ничего вне папки запуска, хост суверенен»). Это правило теперь дисциплинирует все последующие волны.

**Реализовано волнами (агенты o46l → потом crush, с независимым ревью + zero-trust перепроверкой координатором + коммитом на каждую):**
- Этап 1 «периметр честности» (4 коммита): guard блокирует прямую правку baselines/пина, хэш-пин 5 enforcement-файлов (сам себя пинит), 36 fires-тестов («ENFORCED = доказуемо кусается»), AST-резолв enforced_by (не грепом текста), gate_status fail-closed на пустом логе.
- Этап 2 «кристалл-диета»: правило «краткая версия вместо механической обрезки» (`short_form()` хелпер, поле `Requirement.summary`) + применение — кристалл 78.5k→25.7k chars (закон «дерева ссылок» исполнен).
- Этап 3 «живой сенсориум»: автоархивация landed-предложений, дельта-показ (полный список только при изменении, иначе счётчик), порог revisit 5%-от-SETTLED вместо фиксированных +10.
- Быстрая волна (5 мелочей): двойные хуки (баг), 6 потерянных REJECTED (нормализация тире), uv→python в подсказках, мёртвый код gen_spec, дедуп графа.
- Страж скорости прогонов: самокалибрующийся (среднее первых 5 полных прогонов ×1.2, per-machine, gitignored) — закрыл конфликт `C-ec1ec532` speed-vs-verification как первый прецедент нового правила `R-conflict-resolved-in-members-or-mediator`.
- `R-agent-conduct-is-rules-not-tests`: принцип «работа агента не проверяется кодом → честное PROSE-правило, не фальшивый метр» — снёс мёртвый boot-cite метр (10% соблюдения, никто не читал журнал), оставил само правило PROSE.
- Делегации-файлы: `delegations/DG-<n>.md` (коммитится, история в git) + `spec/tools/delegate.py` (create/close/show/list) заменили идею узла-Delegation в графе (2 DRAFT-атома REJECTED).
- Этап 5.1 Signoff-рекорд: `Signoff` dataclass (decided_by/date/instrument/chosen_variant) — подпись больше не испаряется в gitignored JSON; Variants больше не стираются при HELD→DECIDED; `C-be22cdd1` мигрирован как живой пример.

**Подпись-сессия (вердикты стьюарда, все посажены):**
1. DEL-1 (кампанейская доверенность «реши все вопросы») — закрыта (`--close`), не удалена (история сохранена).
2. Git-привязка подписи `decided_by` — оставлена ОТКРЫТЫМ вопросом в графе (`R-decided-by-verifiable-signature`, OPEN), не решена намеренно.
3. Тикеты (движок `tickets/`) — это для БИЗНЕСА (много пользователей), НЕ для самопостроения; self-домен использует TaskList+чекпоинты+git. Это отменило часть плана "самоизмены" про мёртвые тикеты.
4. Устремления (IMPLEMENTS) без будильника-таймера — стьюард отверг таймер («кодом не проверить»), вместо этого — `R-confront-includes-aspirations`: сверка с целями при каждом изменении, это работа сознания оператора, не метрики.
5. Ролевая модель — ОТЛОЖЕНА СОВСЕМ (задача #44 удалена с доски). Проводка-триггер (допущение "один носит все роли") остаётся в графе на случай появления второго человека.
6. Узел-Delegation в графе — ОТВЕРГНУТ, заменён файлами delegations/ (см. выше).

**Переключение на crush.** Стьюард ввёл `/crush` (внешний CLI `crush run`, delegation via `.crush/stdin/*.prompt` → `crush run --role smart --session <id> --timeout 60m --json`). Координатор обязан: запускать в фоне, вести zero-trust верификацию (перечитать дифф, перепрогнать тесты САМ, не доверять конверту), использовать `delegate.py` для DG-файлов на каждую делегацию. Первые делегации через crush прошли успешно (DG-2 ревью делегаций-волны, DG-3/DG-4 волна Signoff — DG-3 сессия оборвалась по rate-limit провайдера ближе к концу, но работа была уже фактически завершена; координатор проверил T2 лично перед тем как считать готовым).

**Текущее состояние графа:** 227 SETTLED / 198 ENFORCED, 2 DRAFT, 1 OPEN (`R-decided-by-verifiable-signature`), 5 closeable debt. Кристалл ~26k chars (было 78.5k). Пульс чист (attention: 1 сигнал — намеренно открытый вопрос).

**Репо:** 1 коммит впереди origin (5fdaae3, НЕ запушен — стьюард прервал на /checkpoint до push). Дерево чисто.

## Active goal

none

## TaskList

### pending
- #65 Этап 5.2 — Время в узлах + распад IMPLEMENTS
- #66 Этап 5.3 — replaces-ребро + write-path дыры протокола (blockedBy: #65)
- #67 Этап 6 — Инженерная плоскость, обзорная (blockedBy: #66)

### recently completed
- #64 Этап 5.1 — Signoff-рекорд (5fdaae3)
- #71 Делегации-файлы (ed1dbab)
- #70 Страж скорости прогонов (5bd9cc2)
- #68 [ВОПРОС СТЬЮАРДУ] git-подпись decided_by — разрешён как OPEN в графе, не как таск-решение
- #56 Самоизмены как Conflict-ноды — реализовано частично: C-ec1ec532 закрыт, boot-cite снесён, delegation-node решён; полный список самоизмен из ревью НЕ исчерпан построчно (T1-T3 синтеза остаются справочным материалом)
- #51 Дельта-сенсориум (7248109)
- #39, #44 (#44 удалена — ролевая модель отложена совсем)

### deleted
- #44 Ролевая модель — стьюард: «нужно совсем отложить, удали это пока»

## Decisions

- Хост (~/.claude, cah) — суверенен, framework никогда его не трогает (R-work-within-launch-dir); снесён весь механизм patch-global.
- Скорость прогонов — самокалиброванный per-machine gitignored порог (mean первых 5 ×1.2), не хардкод-константа.
- Работа агента (высшая абстракция) кодом не проверяется — либо честное PROSE-правило, либо не заводим фальшивый метр (R-agent-conduct-is-rules-not-tests). Boot-cite метр снесён по этому принципу.
- Делегации задач = файлы в git (delegations/DG-n.md), НЕ граф-узел. Узел-Delegation REJECTED.
- Тикет-движок — только для бизнес-доменов (много исполнителей), не для self-построения.
- Git-подпись decided_by — оставлена открытым вопросом намеренно, не форсирована.
- Ролевая модель — отложена совсем, не просто "в конце сессии".

## Open questions

- `R-decided-by-verifiable-signature` (OPEN в графе): привязывать ли decided_by к git-authorship / криптоподписи, или оставить свободной строкой доверенной ревью. Ждёт либо появления второго стьюарда, либо решения по онтологии Signoff.
- Продолжать ли волны #65→#66→#67 через crush в следующей сессии? Стратегия (agent o46l vs crush) не зафиксирована как постоянная — стьюард переключался между ними по ходу.
- Пуш 1 коммита (5fdaae3) — не выполнен, ждёт явного слова.

## Repo state

```
(clean tree)
```
```
5fdaae3 feat: Signoff record -- decided_by/date/instrument/chosen_variant now persist into the graph (no longer lost in gitignored JSON); HELD->DECIDED preserves Variants; C-be22cdd1 migrated as live example (etap 5.1)
ed1dbab feat: file-based delegations -- delegations/DG-<n>.md versioned in git + delegate.py (create/close/show/list) + R-delegation-is-a-file; Delegation graph-node line REJECTED (resolver: files over nodes)
798297e feat: R-agent-conduct-is-rules-not-tests -- agent conduct is rules not fake metrics; boot-cite form-meter excised (tool+hook+27 tests, rule stays honest PROSE); spawn-log atom landed STRUCTURAL; sensorium claim refreshed
5bd9cc2 feat: run-speed guard -- R-run-speed-guarded (self-calibrating: mean of first 5 local runs +20%, per-machine gitignored baseline, full-suite runs only) + C-ec1ec532 speed-vs-verification DECIDED via mediator rule (first precedent of R-conflict-resolved-in-members-or-mediator) + DEL-1 closed + OPEN R-decided-by-verifiable-signature
7248109 feat: living sensorium -- auto-archive landed proposals (no more false 'awaiting resolver'), delta display (full list only on change, else counter), revisit threshold 5% not fixed +10 (etap 3)
```
