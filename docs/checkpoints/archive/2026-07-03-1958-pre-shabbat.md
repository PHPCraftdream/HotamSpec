# Checkpoint — 2026-07-03 19:58 [pre-shabbat пауза]

## Session summary

Пауза перед Шаббатом. За сессию выполнены ДВА конвейера: волны 10-13 (запушены d84f718..1e31b15) и новый план из подпись-сессии — волны 14-17 (движок тикетов / attention / раздел тестов). Модель переключена стьюардом на Opus 4.8; агенты волн: sh→oh→om.

СОСТОЯНИЕ КОММИТОВ:
- ЗАПУШЕНО: до 1e31b15 (волны 10-13) + чекпоинты 2776f90.
- ЗАКОММИЧЕНО НО НЕ ЗАПУШЕНО (3 коммита впереди origin): b8ec67e (подпись-волна 2 — IMPLEMENTS + вердикты), 26faded (волна 15 движок тикетов), и чекпоинт 2776f90.
- НЕ ЗАКОММИЧЕНО (рабочее дерево): Волна 16 «attention-ядро» ПОЛНОСТЬЮ РЕАЛИЗОВАНА (1000 passed, 195/217 ENFORCED, gate SATISFIED, детерминизм подтверждён агентом-исполнителем) но ЕЁ РЕВЬЮ ПРЕРВАНО стьюардом на паузу. Файлы: spec/src/hotam_spec/attention.py (ядро), tools/attention.py + attention_hook.py, 4 атома R-attention-*, §Attention, рефакторинг what_now.diagnose (тонкий адаптер над attention.diagnose_signals), .claude/settings.json (attention_hook на UserPromptSubmit). tmp/ — untracked чужой scratch, не трогать.

ПОДПИСЬ-СЕССИЯ (#39, завершена по существу) — 10 вердиктов стьюарда, все verbatim в узлах:
1. НОВЫЙ СТАТУС ДОПУЩЕНИЙ IMPLEMENTS (волевое устремление, не эпистемический факт) — реализован; переходы кроме →UNCERTAIN требуют подписи.
2. A-prose-suffices → DEAD («тексты — воздух без земли»); замена A-text-grounded-in-models (HOLDS, земля = модель+короткие методы+описание-сверяемое-с-кодом+тесты+стейты), 37 зависимых перепривязаны.
3. A-most-knowledge-crystallizable → IMPLEMENTS; A-bootstrap-self-applies → IMPLEMENTS.
4. Конфликт offload-vs-carry (C-7f86e41d) → DECIDED законом «дерево ссылок» (корень держит только ссылки; уровень полон → уходят в доки 2-го уровня и глубже, рост неограничен).
5. C-80687260 (hotam-dev) → DECIDED «worktree — не параллельность».
6. C-8600b1b8 revisit перезаведён (условие «пятый opt-in аспект ИЛИ первый межаспектный конфликт»).
7. НОВОЕ ПРАВИЛО R-user-request-decomposed-to-tickets (запрос → декомпозиция на тикеты в чате → вопрос куда: session/движок).
8. DEL-1 — «просьба на сейчас», scope «until resolver revokes» + status/expiry-поля в record_delegation.py.
9. Допущение A-single-human-wears-all-hats (HOLDS) — один человек носит все роли.
10. Механизм внимания универсален для любого агента, на стыке API для Claude → Волна 16.

ВЕРДИКТЫ ПО АРХИТЕКТУРЕ (заведены волнами/тикетами):
- Движок тикетов (Волна 15, закоммичен 26faded): tickets/<status>/T-<n>.md, JSON-шапка+MD-тело, вся мутация через ticket_* тулы с авто-историей; 6 первых жителей T-1..T-6 (волны 16/17, ролевая модель, context-хук, tmp/mm, 5 pending).
- attention-ядро (Волна 16, реализована, ждёт ревью): attention ⊇ diagnose (агенту надмножество с fs-сигналами; substrate — детерминированное graph-подмножество).
- Раздел тестов по доктрине «быть рабочим — ответственность фреймворка» (Волна 17, #43, не начата) — закроет находку C3 (тесты хардкодят self-домен).
- Тикеты правятся только тулами (инкапсуляция); ролевая модель — отдельная совместная работа в конце.

Кристалл растёт: 68k→75353 chars (headroom 74647 до warn 130k). Конфликт offload-vs-carry уже не теоретический — при реализации закона «дерева ссылок» самое время.

## Active goal

none (babysit-крон снят перед Шаббатом; /goal не активен)

## TaskList

### in_progress
- #42 Волна 16 — attention-ядро + Claude-адаптер: РЕАЛИЗОВАНА (1000 passed), РЕВЬЮ ПРЕРВАНО на паузу. ПОСЛЕ ШАББАТА: заново запустить om-ревьюера Волны 16 (критично: сверить что what_now.diagnose даёт РОВНО тот же пульс до/после рефакторинга; fs-bands не текут в LIVE-STATE; тест agent-agnostic-core реально ловит platform-токен; attention_hook fail-soft; не дублирует ли attention_hook + emit_cipher инжект) → коммит `feat: wave 16 ...` → затем Волна 17 (#43).

### pending
- #43 Волна 17 — раздел тестов по доктрине ответственности [M] (blockedBy: #42) — закрывает C3
- #39 [КОНЕЦ СЕССИИ] Добить обсуждение: хвост уже закрыт (9б → A-single-human-wears-all-hats HOLDS); осталось напомнить context-хук + tmp/mm + собрать новые находки волн
- #44 [КОНЕЦ СЕССИИ] Продумать ролевую модель вместе со стьюардом (blockedBy: #39)

### completed (эта сессия)
#35-38 волны 10-13, #40 подпись-волна 2, #41 волна 15 движок тикетов

## Decisions

- IMPLEMENTS: снижающие живой сигнал переходы требуют человека, поднимающий (→UNCERTAIN) — нет.
- attention ⊇ diagnose: агент получает надмножество (все fs-сигналы), substrate/CLAUDE.md — только детерминированное graph-подмножество (байт-стабильность сохранена).
- Движок тикетов вместо немедленной DG-Delegation-ноды; онтология ждёт, пока тикеты не покажут нехватку.
- Тикеты: JSON-фронтматтер (не YAML — stdlib-надёжность); мутация только тулами (INHERENTLY_PROSE честно, зубы в R-ticket-carries-history).
- Раздел тестов по ответственности фреймворка вместо «всегда из дома».

## Open questions (для стьюарда после Шаббата)

- Волна 16: подтвердить коммит после ре-ревью (ревью прервано на середине).
- Пуш: 3+ коммита впереди origin (b8ec67e, 26faded, +волна16 после коммита) — по слову.
- Context-хук (личная команда): `cd D:/dev/HotamSpec/spec && uv run python tools/setup_context_hook.py --patch-global --apply` + перезапуск statusline — тикет T-4.
- tmp/mm/ в корне (~109K jira/contract-спеки) — принадлежность? тикет T-5.
- 5 pending-предъявлений (тикет T-6): delegation-node-candidate, R-host-spawn-leaves-trace-land, 3 revisit-маркера.
- Ролевая модель (#44, T-3) — совместная работа в конце.

## Repo state

```
 M CLAUDE.md + docs/gen/* (обоих доменов) + spec/src/hotam_spec/attention.py + tools/attention*.py + what_now.py + tests/test_attention_* + graph.py (4 атома) + .claude/settings.json
?? tmp/  (чужой scratch, не трогать)
(Волна 16 реализована, не закоммичена — ждёт ре-ревью после Шаббата)
```

```
26faded feat: wave 15 -- on-disk ticket engine + resolver queue migrated to disk (191/213)
b8ec67e feat: signature wave 2 -- IMPLEMENTS status + resolver verdicts + decomposition rule + delegation expiry
2776f90 chore: session checkpoint 2026-07-03-1605 -- waves 10-13 pushed + signature verdicts + new plan
1e31b15 feat: wave 13 -- stranger's door: committed sensorium + ProposedStakeholder + honest onboarding (187/207)
a2c4af9 feat: wave 10 -- traces tell truth + two-eyed pulse + closeable debt zero (178/198)
```
