# Checkpoint — 2026-07-14 17:16 [wave-8-in-progress]

## Session summary
Продолжается очень длинная многоволновая сессия по D:\ai_dev\prat\HotamSpec (Go-реализация методологии HotamSpec, репозиторий на branch master, remote PHPCraftdream/HotamSpec — ничего не запушено, всё локально). Установившийся паттерн: пользователь вставляет независимое внешнее ревью через смену модели (`/fl`, `/fm`, `/oh`), я самостоятельно проверяю фактические claims ревью против реального репозитория (никогда не доверяю ревью на слово), декомпозирую принятые находки в план + TaskList с blockedBy-цепочками, и по отдельной явной команде `/babygoal` реализую волну задач через последовательных суб-агентов `/crush` — каждый суб-агент независимо перепроверяется мной (zero-trust) через `go build`, `go vet`, `gofmt -l`, `go test ./...` (обычный, ambient TMP), `go test -race -count=1 ./...` (чистый TMP=D:\ai_dev\_clean_tmp), `all-violations` на обоих доменах (`domains/hotam-spec-self`, `domains/hotam-dev`), плюс ручное воспроизведение сценария бага/фикса на scratch-домене. Коммит — после КАЖДОЙ отдельной подзадачи; push и финальное `@fl`-ревью — только в конце волны, по явному запросу пользователя (push в этой сессии ни разу не запрашивался).

Волна 8 — ответ на 8-е независимое ревью (оценка 8.0/10, найдено 4 P1-проблемы). План записан в `docs/reviews/2026-07-14-review8-response-plan.md` (untracked, по установившейся конвенции сессии). Задачи #144-149 полностью реализованы, проверены и закоммичены (6 коммитов: `afa40f1`, `da86aab`, `7cc2c1a`, `32b4564`, `b6513b9`, `a408992`). Задача #150 (R8-g, P2, генерация PROPOSAL-REFERENCE.md drift-теста из Go-структур) сейчас выполняется суб-агентом `/crush` в сессии `hotamspec-r8g-proposal-reference-drift` — на момент этого чекпойнта сессия ещё РАБОТАЕТ (heartbeat живой, `crush sessions why` вернул `status: running`, файл `.out` пока пуст, `.err` показывает только лог tool-calls). Результат ЕЩЁ НЕ получен и НЕ верифицирован мной. Задача #151 (архивация исторических reviews/checkpoints) заблокирована #150 и не начата.

Между волной 6 и волной 8 также была выполнена мини-волна 7 (2 задачи, коммиты `a402bcc`, `8b4f129`) плюс осознанная регенерация корневого crystal (`58e1c5b`) по явному согласию пользователя через AskUserQuestion. Babysit-крон `086d1eb8` (интервал 15 минут, off-minute `7,22,37,52 * * * *`) активен и подтверждён свежим CronList-вызовом. Стандартная инструкция пользователя в силе: в час-пик (примерно до 12:00 по местному времени, проверять через `date`, не слепым ретраем) переключаться с `/crush` на суб-агента `@sh`.

## Active goal
none (используется TaskList + babysit-крон, не `/goal` Stop-hook)

## TaskList
### in_progress
- #150 R8-g P2: PROPOSAL-REFERENCE.md генерируется из Go-структур (blockedBy: none) — суб-агент crush сессия `hotamspec-r8g-proposal-reference-drift` активна, результат не получен

### pending
- #151 R8-h P2: архивация исторических reviews/checkpoints (blockedBy: #150)

### recently completed
- #149 R8-f: hotam propose axis|assumption|conflict (commit a408992)
- #148 R8-e: унификация дефолтного профиля init/init-project (commit b6513b9)
- #147 R8-d P1: явное подтверждение смысловых противоречий при land (commit 32b4564)
- #146 R8-c P1: контракт --json — ровно один JSON-документ (commit 7cc2c1a)
- #145 R8-b P1: consumer REPO-MAP/crystal без внутренностей фреймворка (commit da86aab)
- #144 R8-a P1: consumer tools/INDEX.md без битых ссылок (commit afa40f1)

## Decisions
- Волна 8 задачи выбраны как реализация всех 4 P1 + 2 из нескольких P2-находок 8-го ревью (R8-a..R8-f обязательны, R8-g/R8-h — низкий приоритет, но включены в ту же волну, а не отложены).
- Task #150: выбран подход "drift-test-only" (расширение существующего `cmd/hotam/proposal_reference_test.go` reflection-проверкой Required/Optional полей), а НЕ полная регенерация документа из struct-тегов — обоснование в промпте задачи: документ несёт смысловую прозу (defaults, cross-references), которую генерация из голых `json:"..."` тегов потеряла бы.
- После task #143 (git stash инцидент суб-агента) — добавлено явное правило "не выполнять деструктивные git-команды" во все последующие промпты задач (#144+).

## Open questions
- Когда пользователь захочет запушить накопленную локальную историю (волна 6 + мини-волна 7 + регенерация crystal + волна 8) — решение не запрошено, стоит на паузе до явного запроса.

## Repo state
```
?? docs/checkpoints/2026-07-14-0611.md
?? docs/reviews/2026-07-14-review8-response-plan.md
```
```
a408992 feat: hotam propose axis|assumption|conflict (task #149, review-8 R8-f)
b6513b9 fix: hotam init defaults to consumer profile, matching init-project (task #148, review-8 R8-e)
32b4564 feat: land refuses to write a semantic conflict without a recorded human decision (task #147, review-8 R8-d P1)
7cc2c1a fix: --json emits exactly one document on stdout, land/propose --land included (task #146, review-8 R8-c P1)
da86aab fix: consumer REPO-MAP/crystal drop framework-internal source references (task #145, review-8 R8-b P1)
```
