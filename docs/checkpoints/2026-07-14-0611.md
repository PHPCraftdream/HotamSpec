# Checkpoint — 2026-07-14 06:11 [review6-wave-started]

## Session summary

This continues a very long multi-wave session on `D:\ai_dev\prat\HotamSpec` (Go implementation of the HotamSpec methodology, git branch `master`, remote `PHPCraftdream/HotamSpec`). Six independent external reviews have now run across this session, each following the established pattern: user pastes review via `/fl`/`/fm`/`/oh`, I independently verify claims against the live repo, decompose into a plan + TaskList, then implement via sequential `/crush` sub-agents with zero-trust re-verification (build/vet/gofmt/test -race in a CLEAN env — `TMP`/`TEMP=D:\ai_dev\_clean_tmp`, since this developer's own home directory `C:\Users\Computer\` has stray `domains/`/`.claude/`/`CLAUDE.md` files from unrelated tooling that falsely satisfy this framework's own project-root marker detection — plus `all-violations` on both domains, gen-spec idempotency), commit after every task, and a final `@fl` review at the end of each wave.

Wave 5 (13 tasks, #118-130) fully landed and committed this session (commits `1925da8`..`d66547f`, NOT pushed — resolver has not requested a push). It covered: unified tool registry, bare-domain gen-spec fix, active-domain resolution (marker/env/`hotam use`), README sync, boolFlagNames AST guard, the burn-down metric split (`blocked_on` field, schema_version 1→2), `hotam propose`, confront-at-gate, `hotam brief`, structural confront (`--proposal`), `--json` for what-now/gate/all-violations + crystal tool-reference trimmed to a pointer one-liner (required a real requirement-supersession proposal, landed properly), `gen-spec --profile consumer|full`, and a chronological index for docs/reviews+checkpoints. The wave's own final `@fl` review scored **8.3/10** (up from 8.0), found zero regressions, but surfaced 3 new findings (N1: `blocked_on` has no clear-sentinel, so a feature-blocked requirement can never return to closeable-now through a proposal; N2: `hotam propose requirement -h` swallows `requirement` as `-h`'s value and shows generic usage instead of flag help; N3: the two new INDEX.md files, committed in task #130, link to two files — the wave-5 plan doc and a checkpoint — that were deliberately left untracked all wave per every task's own "don't touch" instruction, creating dead links in a fresh clone).

A 6th external review (8.0/10, roughly flat vs the internal @fl score) was pasted via `/fm` and independently verified: **P0 confirmed by direct reproduction** — a PLAIN `go test ./...` (no TMP override) is actually RED on this machine, because two tests (`TestRepoRootForDomain_NoProjectRootFallsBackToDomainDir` in `gen_spec_test.go`, `TestExternal_InitGenSpecOutsideAnyProject_BareDomain` in `external_e2e_test.go`) build an "isolated" `t.TempDir()`/`os.MkdirTemp` working directory that on THIS machine resolves ~5 levels below the contaminated `C:\Users\Computer\` — exactly `internal/paths.ProjectRoot()`'s `MaxMarkerSearchDepth`, so the upward marker search reaches the stray home-dir markers and resolves a project root the tests explicitly expect to NOT exist. Also confirmed: P1 profile-switch doesn't delete stale files (full→consumer leaves old thinking/*.md and Planned tool pages on disk despite a smaller `written` list in stdout), P1 `land` without `--claude-md` leaves the root crystal stale even though graph+docs are current, P1 consumer-profile crystal references paths (`docs/gen/thinking/`, etc.) that don't exist under the consumer profile, P1 flag inconsistency (`propose stakeholder`'s `--domain` names the Stakeholder's own business-domain field while `--domain-dir` is the graph path, opposite of every other `propose` subcommand; `apply-proposal`/`land` still require explicit `--domain` even though `status`/`brief`/`gen-spec` already resolve active-domain), and doc drift (README "fifteen commands" vs 17 real, `--profile`/`confront --proposal` undocumented, QUICKSTART-CONSUMER.md still walks through the old manual `init` instead of `init-project`, `internal/generator/repomap_data.go` self-admits its Tools section is hand-maintained and already stale).

A wave-6 plan was written (`docs/reviews/2026-07-14-review6-response-plan.md`, currently UNTRACKED, same as prior wave-plan docs) covering 11 tasks (#131-141): P0 test hermeticity first, then the wave-5 @fl findings (N1/N2/N3) folded in as tasks #138/#139/#132, then the 6th review's P1 items in dependency order, ending with the two lowest-priority polish items (consumer REQUIREMENTS.md size, inspect noise reduction). `/babygoal` was invoked to execute it; babysit is armed (job `6ce56c6e`, every 15m). **A user side-question mid-wave** ("does our tool already generate CLAUDE.md/AGENTS.md/GEMINI.md from one template?") was answered directly without creating a task — confirmed YES, already true since early this session (`genSpec`'s crystal-write step in `cmd/hotam/gen_spec.go` renders once via `RenderClaudeMDFromTemplate` and writes identical bytes to all three paths).

**Currently in flight**: task #131 (P0 test hermeticity) is running via a `/crush` sub-agent (session `hotamspec-r6a-hermetic-tests`), NOT yet returned/verified. The design direction given to the sub-agent (decided by me before delegating, not left to its own judgment): do NOT hardcode any machine-specific "known clean" path into committed tests (not portable); instead add a new exported read-only diagnostic `internal/paths.DetectAncestorMarker(start string)` reusing the existing marker-detection primitives, and convert the two failing tests' isolation check from a hard `t.Fatalf` precondition (which reads as FAIL) to an honest `t.Skip` with a clear message naming the offending ancestor+marker — so plain `go test ./...` is GREEN (skip, not fail) on a contaminated machine, while the real assertion still runs and passes when the ancestry is genuinely clean (verified by re-running with `TMP=D:\ai_dev\_clean_tmp`). The sub-agent was also asked to audit for the same vulnerability shape elsewhere (likely `init_project_e2e_test.go` too) and apply the same guard. **Not yet independently verified by me** — the last `git status --short` showed new untracked scratch artifacts (`d/`, `tmp/`, `internal/paths/zprobe_test.go`) that look like sub-agent working files, not yet resolved either way.

## Active goal

A session-scoped Stop-hook goal is in force (set implicitly by `/babygoal`'s invocation this turn): *"реализуй задачи, друг, используй агентов /crush для этого, между тасками делай коммиты, в конце сделай ревью с помощью @fl"* — i.e. execute all remaining wave-6 TaskList items via sequential `/crush` sub-agents, commit after each (with independent zero-trust verification first), and run a final `@fl` review once the TaskList empties. This hook will keep blocking session-stop until the TaskList is empty (babysit's own stop condition matches).

## TaskList

### in_progress
- #131 R6-a P0: герметичность тестов от домашнего окружения — зелёный go test без чистого TMP (no blockers; currently running via /crush, not yet verified/committed)

### pending
- #132 R6-b: закоммитить untracked-файлы, на которые ссылаются INDEX.md (N3) (blockedBy: #131; NOTE — orchestrator does this one directly, no sub-agent, per its own task description)
- #133 R6-c P1: очистка stale-файлов при смене профиля gen-spec (blockedBy: #132)
- #134 R6-d P1: land автоматически обновляет root crystal (blockedBy: #133)
- #135 R6-e P1: профильные ссылки в кристалле + link-existence test (blockedBy: #134)
- #136 R6-f P1: унификация --domain + active-domain для apply-proposal/land (blockedBy: #135)
- #137 R6-g P1: README/QUICKSTART/REPO-MAP из registry + drift-тесты (blockedBy: #136)
- #138 R6-h: clear-сентинел для blocked_on — N1 из финального @fl-ревью волны 5 (blockedBy: #137)
- #139 R6-i: починить -h/-help в hotam propose — N2 из финального @fl-ревью волны 5 (blockedBy: #138)
- #140 R6-j P2: сжать consumer REQUIREMENTS.md (blockedBy: #139)
- #141 R6-k: снизить шум inspect — 249 кандидатов → ранжирование структурных сигналов (blockedBy: #140, last task)

### recently completed
(all wave-5 tasks #118-130 — see `docs/checkpoints/2026-07-13-1910.md` and the git log for detail; not re-listed here to keep this checkpoint focused on wave 6)

## Decisions

- Chose "detect contamination + `t.Skip` with an actionable message" over "hardcode a known-clean path" for task #131 — the latter is machine-specific and non-portable (would break on any other dev machine or CI runner with different contamination, or none at all); the former is honest about what the test precondition actually requires and turns a false-FAIL into an honest, diagnosable SKIP without weakening the real assertion when the environment genuinely permits it.
- Folded the wave-5 final `@fl` review's 3 findings (N1 blocked_on clear-sentinel, N2 propose -h bug, N3 dead INDEX links) directly into the wave-6 TaskList (#138, #139, #132) rather than spinning a separate mini-wave — they're small, well-understood, and the wave-6 plan was being written anyway.
- Sequenced wave-6 P0 (#131) strictly first (test hermeticity blocks trusting every subsequent task's own verification), then the N3 dead-link fix (#132, cheap, orchestrator-direct, no sub-agent) before the larger P1 items — established precedent: fix visible small honesty gaps before larger structural work.
- Declined to create a task for the "one template → CLAUDE.md/AGENTS.md/GEMINI.md" user question — verified it's already implemented (confirmed via reading `cmd/hotam/gen_spec.go`'s existing crystal-write code), answered directly, no new work needed.
- Release tag / binary publication (review 6's item 8) again deliberately left OUT of the wave-6 plan — resolver has declined it multiple times this session; will be raised again only after P0/P1 items close, on the resolver's own initiative.

## Open questions

- None outstanding from the user — wave-6 plan was presented and `/babygoal`'d without pushback. The single latent technical uncertainty is task #131's exact sub-agent implementation (in flight, not yet reviewed by me) — will be resolved by my own independent verification once the sub-agent returns, not a resolver-facing question.
- Whether to push the now-substantial unpushed commit chain (`1925da8`..`d66547f`, 12+ commits, wave 5 in full) before or after wave 6 lands — not asked, not decided; stays local per the standing "never push without explicit request" rule until the resolver asks.

## Repo state

```
?? d/
?? docs/checkpoints/2026-07-13-1910.md
?? docs/reviews/2026-07-13-review5-response-plan.md
?? docs/reviews/2026-07-14-review6-response-plan.md
?? internal/paths/zprobe_test.go
?? tmp/
```

```
d66547f docs: chronological index for docs/reviews and docs/checkpoints (task #130, review-5 R5-h)
6755697 feat: gen-spec --profile consumer|full, init-project defaults to consumer (task #129, review-5 R5-g)
309138e feat: --json for what-now/gate/all-violations + crystal tool-reference pointer trim (task #128, review-5 R5-m)
eae4d27 feat: structural confront for proposals — shared-assumption + axis signals (task #127, review-5 R5-l)
9ecb34b feat: hotam brief <id> — single-call orientation over show+context+related+due (task #126, review-5 R5-k)
```

Note: `d/`, `tmp/`, and `internal/paths/zprobe_test.go` are almost certainly working artifacts from task #131's still-running `/crush` sub-agent (session `hotamspec-r6a-hermetic-tests`) — not yet resolved either way (could be intentional scratch the sub-agent will clean up, or leftovers to flag during verification). Do not assume either way; check fresh on resume.
