# Checkpoint — 2026-07-03 19:59 [pre-shabbat, авто]

## Session summary

Пауза перед Шаббатом (дубль pre-shabbat-чекпоинта под таймстамп-именем). Модель переключена стьюардом на Opus 4.8; агенты волн: sh→oh→om. За сессию — два конвейера: волны 10-13 (запушены) и новый план из подпись-сессии (волны 14-17: движок тикетов / attention / раздел тестов).

СОСТОЯНИЕ КОММИТОВ:
- ЗАПУШЕНО: до 1e31b15 (волны 10-13).
- ЗАКОММИЧЕНО, НЕ ЗАПУШЕНО (впереди origin): 2776f90 (чекпоинт), b8ec67e (подпись-волна 2), 26faded (волна 15 движок тикетов).
- НЕ ЗАКОММИЧЕНО: Волна 16 «attention-ядро» ПОЛНОСТЬЮ РЕАЛИЗОВАНА (1000 passed, 195/217 ENFORCED, gate SATISFIED, детерминизм подтверждён исполнителем), но ЕЁ РЕВЬЮ ПРЕРВАНО стьюардом на паузу. tmp/ — чужой untracked scratch, не трогать.

ПОДПИСЬ-СЕССИЯ (10 вердиктов стьюарда, все verbatim в узлах):
1. Новый статус допущений IMPLEMENTS (волевое устремление, не факт); переходы кроме →UNCERTAIN требуют подписи.
2. A-prose-suffices → DEAD («тексты — воздух без земли»); замена A-text-grounded-in-models (HOLDS: модель+короткие методы+описание-сверяемое-с-кодом+тесты+стейты), 37 зависимых перепривязаны.
3. A-most-knowledge-crystallizable → IMPLEMENTS; A-bootstrap-self-applies → IMPLEMENTS.
4. Конфликт offload-vs-carry (C-7f86e41d) → DECIDED законом «дерево ссылок».
5. C-80687260 (hotam-dev) → DECIDED «worktree — не параллельность».
6. C-8600b1b8 revisit перезаведён; механизм внимания универсален для любого агента, API для Claude на стыке.
7. Правило R-user-request-decomposed-to-tickets.
8. DEL-1 «просьба на сейчас» + status/expiry в реестре.
9. A-single-human-wears-all-hats (HOLDS).
10. Движок тикетов на диске вместо немедленной DG-Delegation-ноды.

Кристалл растёт: 75353 chars (headroom 74647 до warn 130k) — закон «дерева ссылок» из вердикта 4 скоро понадобится.

## Active goal

none (babysit-крон снят перед Шаббатом; /goal не активен)

## TaskList

### in_progress
- #42 Волна 16 — attention-ядро + Claude-адаптер: РЕАЛИЗОВАНА (1000 passed), РЕВЬЮ ПРЕРВАНО. ПОСЛЕ ШАББАТА: заново запустить om-ревьюера (критично: пульс what_now.diagnose РОВНО тот же до/после рефакторинга; fs-bands не текут в LIVE-STATE; тест agent-agnostic-core ловит platform-токен; attention_hook fail-soft; не дублирует ли attention_hook+emit_cipher инжект) → коммит → Волна 17.

### pending
- #43 Волна 17 — раздел тестов по доктрине ответственности [M] (blockedBy: #42) — закрывает находку C3 (тесты хардкодят self-домен)
- #39 [КОНЕЦ СЕССИИ] Добить обсуждение: хвост 9б закрыт (A-single-human-wears-all-hats); осталось напомнить context-хук + tmp/mm + собрать новые находки
- #44 [КОНЕЦ СЕССИИ] Продумать ролевую модель со стьюардом (blockedBy: #39)

### recently completed
- #40 подпись-волна 2 (b8ec67e), #41 движок тикетов (26faded), #35-38 волны 10-13 (запушены)

## Decisions

- IMPLEMENTS: снижающие живой сигнал переходы требуют человека, поднимающий (→UNCERTAIN) — нет.
- attention ⊇ diagnose: агент получает надмножество (fs-сигналы), substrate/CLAUDE.md — детерминированное graph-подмножество.
- Движок тикетов вместо DG-Delegation-ноды; онтология ждёт, пока тикеты не покажут нехватку.
- Тикеты: JSON-фронтматтер (не YAML), мутация только тулами (INHERENTLY_PROSE честно, зубы в R-ticket-carries-history).
- Раздел тестов по ответственности фреймворка вместо «всегда тестировать из дома».

## Open questions

- Волна 16: подтвердить коммит после ре-ревью (ревью прервано на середине).
- Пуш: 3 коммита впереди origin (+волна16 после коммита) — по слову стьюарда.
- Context-хук (личная команда): `cd D:/dev/HotamSpec/spec && uv run python tools/setup_context_hook.py --patch-global --apply` + перезапуск statusline — тикет T-4.
- tmp/mm/ в корне (~109K) — принадлежность? тикет T-5.
- 5 pending-предъявлений (тикет T-6); ролевая модель (#44, тикет T-3) — в конце.

## Repo state

```
 M CLAUDE.md + docs/gen/* (оба домена) + spec/src/hotam_spec/attention.py + tools/attention*.py + what_now.py + tests/test_attention_* + graph.py (4 атома) + .claude/settings.json
?? tmp/  (чужой scratch, не трогать)
(Волна 16 реализована, не закоммичена — ждёт ре-ревью после Шаббата)
```

```
26faded feat: wave 15 -- on-disk ticket engine + steward queue migrated to disk (191/213)
b8ec67e feat: signature wave 2 -- IMPLEMENTS status + steward verdicts + decomposition rule + delegation expiry
2776f90 chore: session checkpoint 2026-07-03-1605 -- waves 10-13 pushed + signature verdicts + new plan
1e31b15 feat: wave 13 -- stranger's door: committed sensorium + ProposedStakeholder + honest onboarding (187/207)
a2c4af9 feat: wave 10 -- traces tell truth + two-eyed pulse + closeable debt zero (178/198)
```
