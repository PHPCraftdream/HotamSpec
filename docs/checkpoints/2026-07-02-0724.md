# Checkpoint — 2026-07-02 07:24

## Session summary

Пользователь работает над Hotam-Spec — executable-методологией для противоречивых бизнес-требований (tension graph, contradiction-as-node). Сессия началась с того, что корневой CLAUDE.md пробил 150k-символьный лимит хоста Claude Code (был 200,251 символов), и выросла в полную перестройку «мышления» оператора.

Путь сессии: (1) research состава мышления (oxx) → (2) Tier-1 дистилляция EMBEDDED-блоков (полный текст ~135k → RULE+WHY-дистилляты ~18k) → (3) созерцания вскрыли корневые диагнозы: граф на 96% инертен (6 конфликтов на ~193 требования, 0 relations) — самохостинг без внешнего референта; спекулятивная общность (Entity при 0 инстансов, федерация при 1 домене, суб-агенты при 0 активных); счётчик бюджета NODE_COUNT мерил бесплатный субстрат вопреки R-working-vs-substrate-budget, а φ-cap был захардкожен и лгал → (4) пятифазный план реализован агентами sh.

Реализовано и закоммичено (a6459b3, запушено): мера бюджета CRYSTAL_CHARS (символы резидентного кристалла против потолка хоста 150k/130k warn) + новый вид proposal OperatorBudget; claim=реальность (старые embeds-требования REJECTED, заменены *-distilled); семя-блоки OPERATOR-ROLE / MEDIATION-LOOP (ORIENT→LOCATE→CONFRONT→TRANSLATE→PRESENT→LAND) / OPERATOR-RECURSION в caveman-стиле; CONSTITUTION → индекс (строка на требование); партиция 113 framework-plumbing атомов → FRAMEWORK-INVARIANTS.md, 43 бизнес+дисциплина остались резидентно; R-speculative-aspects-frozen (SETTLED/STRUCTURAL). Итог: CLAUDE.md 200,251 → 53,440 символов (−73%), 539 passed / 1 skipped, детерминизм byte-identical.

Целевая роль оператора, сформулированная пользователем: страж непротиворечивости спека↔тестов↔бизнеса, проверяльщик гипотез против реальности, компактное мышление (семя+ссылки), рекурсивное порождение таких же под-агентов на под-области.

Найден и задокументирован реальный gap (задача #15): apply_proposal не может адресовать ни один существующий Conflict-узел (резолвит только литеральные axis/context, а граф использует локальные переменные) — ACT-половина петли для конфликтов нерабочая. Также попутно починен баг детектора импорта Relation в apply_proposal.py.

Правила сессии от пользователя: под-агентов вызывать только по явной просьбе (команды /oxx /sm /sh /sl /fxx = явные просьбы); коммит/пуш только по явной просьбе (выполнено); graph.py только через apply_proposal; Фаза 5 (реальный домен) отложена явно («5 фазы пока не будет»); φ-cap-часть старой задачи #4 была отложена, затем реализована в составе фикса меры.

## Active goal

none (нет /goal Stop hook; babysit-cron был установлен и снят в течение сессии)

## TaskList

### in_progress
(нет)

### pending
- #11 Фаза 5 — Посадить реальный бизнес-домен (внешний референт) — ждёт явного сигнала стьюарда
- #12 [BACKLOG] Инструмент tools/check_hypothesis.py (blockedBy: #11)
- #13 [BACKLOG] Table-driven инварианты + дистилляция gen_spec.py (blockedBy: #11)
- #14 [BACKLOG] Аудит: каждый generated-док называет своего читателя
- #15 [BACKLOG] apply_proposal: адресация Conflict-узлов через переменные axis/context — реальный баг, не заблокирован, кандидат на следующий шаг

### recently completed
(удалены пользователем командой «удали выполненные таски» — 10 задач: research мышления, выбор стратегии, драфты proposals, фикс меры, дистилляция, классификация plumbing/бизнес, Фазы 1–4)

## Decisions

- Выбрана тирированная архитектура кристалла (семя = один закон + петля + живая кромка; полные тексты по Tier-3-ссылкам) вместо A/B/C-вариантов простой обрезки — «принципы ≠ оглавление, семя ≠ каталог».
- Мера бюджета: починить весы (CRYSTAL_CHARS) вместо поднятия лимита или допущения ложного OVERLOADED — NODE_COUNT мерил субстрат, объявленный бесплатным.
- Caveman-стиль для всех резидентных блоков (читает модель, не человек); границы: НЕ применять к claims требований и RULE+WHY-дистиллятам.
- Заморозка Entity/федерации/суб-агентов (не удаление) до реального спроса; выбран Вариант 2 (атом без Conflict-transition) из-за gap инструмента.
- check_hypothesis.py отложен до Фазы 5 (YAGNI: на 96%-инертном графе инструменту нечего считать).

## Open questions

- Запуск Фазы 5 (реальный бизнес-домен) — единственный разблокирующий сигнал для #12/#13; пользователь явно отложил.
- Браться ли за #15 (fix адресации конфликтов) следующим — предложено, не отвечено.
- ~20 «двусмысленных» атомов из классификации plumbing/бизнес получили дефолтные вердикты (консервативно оставлены в бизнес-индексе: R-conflict-structurally-visible, R-conflict-min-two-members, R-decided-conflict-justifies-itself и др.) — пользователь не проходил список построчно.

## Repo state

```
(clean — все изменения закоммичены и запушены)
```

```
a6459b3 feat: crystal budget honesty + tiered seed + plumbing relocation + aspect freeze
23d6ce5 feat: CLAUDE.md.template.txt with mind/business placeholders (merge B2+D1+C1)
329e8a1 feat: template-driven CLAUDE.md generation -- <!-- mind --> and <!-- business --> placeholders
359c76a chore: regen docs after B2+D1 merge, remove D1 worktree
acaf776 Merge branch 'worktree-agent-a8c2e429f56f90e9c'
```
