# Checkpoint — 2026-07-01 16:00 [P22-consolidation-and-github-launch]

## Session summary

This session continued from a prior compact and covered a long arc: closing the sensor-substrate gap (P22), launching the project publicly on GitHub as "Hotam-Spec" with open-source standards, then a deep architectural correction the director drove personally — collapsing what had become a 4-file CLAUDE.md sprawl (root + domain + director-agent + framework-agent scaffold) down to exactly ONE CLAUDE.md at repo root, generated from a human-editable template with two placeholders.

Early in the session, P22 wave work closed the "operator boots from raw session memory, not substrate" gap: hooks now regenerate CLAUDE.md at SessionStart/PostCompact, a `UserPromptSubmit` hook (`emit_cipher.py`) injects the three-cipher pulse every turn (visibly working — every reply in this session opened with a live pulse citation), and `spawn_agent.py` composes a sub-agent's prompt by prepending its CLAUDE.md crystal. A `R-no-hand-edit-graph` PreToolUse guard was hardened from LLM-judgment (`type:"prompt"`, bypassable) to a deterministic `type:"command"` hook (`_graph_guard.py`) that blocks direct edits to `domains/*/graph.py` — proven live during this session when it correctly blocked the operator's own attempt to hand-splice a merge conflict.

A serious incident occurred and was resolved honestly: the director ran `git filter-repo` to purge `.idea/`/`__pycache__` from git history (to prepare for a public GitHub push) without first committing an in-flight parallel sm-agent's work, which wiped that agent's uncommitted P5-noise-fix code. The work was identified, redone from scratch by a fresh agent, and the incident was crystallized into `R-parallel-mutating-agents-use-worktree` (SETTLED/STRUCTURAL) — a governing rule that parallel mutating Agent-tool calls should use `isolation:'worktree'` going forward, which the director then adopted for all subsequent multi-agent work in this session.

The repo was created public at `https://github.com/PHPCraftdream/HotamSpec` and pushed (including a history rewrite via `git filter-repo` to remove `.idea/__pycache__` from all commits — force-pushed after explicit user confirmation). Open-source scaffolding was added: dual MIT/Apache-2.0 LICENSE, README.md, CONTRIBUTING.md, PR/issue templates, GitHub topics.

The director then investigated a real gap: `SHARED-DOCS`/`THINKING-INDEX` blocks in CLAUDE.md were markdown LINKS, not embedded content — Claude Code does not follow links when loading CLAUDE.md, so the operator only knew "where docs are," not their content. This was fixed (task A1) by embedding full thinking-doc bodies inline. The director then made a bigger call mid-flight: since only ONE agent is actually active (framework-agent was a dormant P21 demo, never spawned), the whole per-domain/per-agent CLAUDE.md file split was premature — consolidated (tasks B1+B2) to exactly one CLAUDE.md at repo root, deleting the domain file and the agent scaffold tree entirely (keeping only a bare `scope.py` identity marker for `check_domain_director_exists`). A companion insight from the director — "spec should contain all docs, not links" and "every sub-agent inherits base thinking + parent connection + own business" — was recorded as `R-sub-agent-crystal-triad` (STRUCTURAL, governs the FUTURE shape when a real second agent is spawned).

The director also asked "how would I know a file changed mid-session?" and the answer became a new tool: `claude_md_diff_watch.py` (task D1) — a `UserPromptSubmit` hook that diffs CLAUDE.md against a snapshot each turn and injects only the delta (capped, with a "re-read the file" fallback for large changes) rather than re-injecting the whole ~200KB file every turn.

Finally, the director asked for a template mechanism so a human can write durable plain-text notes that regeneration will never clobber — task C1 built `CLAUDE.md.template.txt` with two literal placeholders, `<!-- mind -->` (methodology layer: REPO-MAP + THINKING-INDEX + EMBEDDED-THINKING + EMBEDDED-TOOLS) and `<!-- business -->` (domain-claims layer: LIVE-STATE + DOMAIN-MAP + CONSTITUTION + AGENT-MAP + CONCEPT-MAP + RECENTLY-REJECTED). This branch was launched before B1/B2 fully propagated, causing a real merge conflict (C1 still referenced the deleted domain CLAUDE.md file and had duplicate/dead function definitions after merge). The director resolved this manually and carefully: kept the B1/B2 deletion decision, re-added C1's one new requirement via `apply_proposal` (not hand-splicing graph.py — correctly blocked once by the PreToolUse guard), removed duplicate `_render_embedded_thinking_block`/`_render_embedded_tools_block` definitions in `gen_spec.py` (Python was silently using the later/broken one, missing the `#### <name>` headings the anti-drift tests expected), and fixed test files that referenced undefined names left over from C1's stale branch point. Final state: 515 passed, 1 skipped, committed as `23d6ce5`.

**Not yet pushed to GitHub** — the director explicitly wants to confirm before every push per their global CLAUDE.md rule; last push was before this session's newest commits (P22 close-out through the C1 merge resolution).

## Active goal

None — no `/goal` Stop hook is currently armed. All work this session was driven by `/babygoal`/direct requests with `/babysit` cron heartbeats, all of which self-stopped when their TaskLists emptied.

## TaskList

Empty. All tasks (#94–#104 across this session, covering P5-noise fix, enforceability flag, worktree-discipline recording, EMBEDDED-THINKING, single-CLAUDE.md consolidation, diff-watch hook, and the template mechanism) are completed.

## Decisions

- **Consolidated to exactly ONE CLAUDE.md file** (repo root) over keeping the P17-era per-domain/per-agent file split — chose this because only one agent (director) is actually active; `create_agent.py`/`spawn_agent.py` remain fully functional for when a real second agent is spawned (per `R-claude-md-consolidates-when-single-agent` and `R-sub-agent-crystal-triad`).
- **Embedded full methodology/tool content inline** (EMBEDDED-THINKING/EMBEDDED-TOOLS) over keeping markdown links — links are inert to Claude Code's CLAUDE.md loader; only inline content actually reaches the operator's context.
- **Template-substitution model** (`CLAUDE.md.template.txt` with two placeholders) over the prior ~10-sentinel surgical-splice approach — simpler mental model, and critically gives a human a durable plain-text zone regeneration will never touch.
- **Hardened the graph-edit guard hook from LLM-judgment to deterministic command** — a `type:"prompt"` hook can be talked around by phrasing; a `type:"command"` pattern-match cannot.
- **Recorded the filter-repo/worktree incident as a SETTLED requirement** rather than just fixing the immediate damage — `R-parallel-mutating-agents-use-worktree`, and adopted `isolation:'worktree'` for every subsequent parallel-agent launch this session.
- **Resolved the C1/B1/B2 merge conflict by hand, favoring the tested master state** over C1's stale-branch assumptions, re-adding C1's genuinely new contribution (the template mechanism + its one new requirement) via the sanctioned `apply_proposal` tool rather than bypassing the graph-edit guard.

## Open questions

- **Push to GitHub not yet done this session** — director has not yet said "push." All commits through `23d6ce5` are local-only relative to `origin/master`.
- **7 genuinely OPEN requirements remain** (axis-gatekeeper-policy, backend-scope, budget-measure, observation-evidence-scope, partition-vs-border, rules-as-data, trust-anchor-mechanism) — these are steward decisions, not something the operator can resolve alone.
- **27 SETTLED-unenforced ("closeable debt")** remain per the current three-cipher pulse — a future burn-down wave could reduce this further (last wave took it from 35→26 before this session's work nudged it back up slightly as new atoms were added).
- **No second real business domain exists yet** — the whole domain/entity/agent machinery (P17, P21, and now P22's consolidation-until-needed model) has only ever been dogfooded on the meta-domain itself; a real stress test awaits an actual second domain or a real spawned agent.

## Repo state

```
(clean — no uncommitted changes)
```

```
23d6ce5 feat: CLAUDE.md.template.txt with mind/business placeholders (merge B2+D1+C1)
329e8a1 feat: template-driven CLAUDE.md generation -- <!-- mind --> and <!-- business --> placeholders
359c76a chore: regen docs after B2+D1 merge, remove D1 worktree
acaf776 Merge branch 'worktree-agent-a8c2e429f56f90e9c'
086d557 feat: claude_md_diff_watch.py — auto-inject CLAUDE.md diff into session on change
```
