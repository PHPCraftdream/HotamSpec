# Checkpoint — 2026-07-13 19:10 [review5-planned]

## Session summary

This is the continuation of a very long multi-wave session on `D:\ai_dev\prat\HotamSpec` (Go implementation of the HotamSpec methodology, git branch `master`, remote `PHPCraftdream/HotamSpec`). Five independent review-response waves have now run this session, each following the same established pattern: the user pastes an external review (via `/fl`), I independently verify its factual claims against the live repo BEFORE trusting them, decompose accepted findings into a written plan file + TaskList items, then implement via sequential `/crush` sub-agents — each sub-agent's work is independently re-verified by me (zero-trust: never accepting a sub-agent's self-report) via `go build`, `go vet`, `gofmt -l`, `go test -race -count=1 ./...` (run in a CLEAN environment — `TMP`/`TEMP` pointed at `D:\ai_dev\_clean_tmp`, deliberately outside both this repo AND this developer's own home directory `C:\Users\Computer\`, which was discovered mid-session to contain stray `CLAUDE.md`+`.claude\`+`domains\` files from unrelated tooling that falsely satisfy `internal/paths`' project-root marker detection and can mask real bugs — this exact contamination caused a CI failure that passed locally before the clean-env discipline was adopted), `go run ./cmd/hotam all-violations --domain <domain>` for both `domains/hotam-spec-self` and `domains/hotam-dev`, and explicit `gen-spec --claude-md`-then-diff-again idempotency checks (a staleness mismatch between the root crystal's debt-count line and `docs/gen/UNENFORCED.md`'s was caught and fixed twice mid-session because a sub-agent's `hotam land` call omitted `--claude-md`).

Waves 1-4 all landed and were pushed to `origin/master` this session (wave 4's final push confirmed CI green at commit `66e4af2`, then a further chain of 9 commits through wave 4's actual content landed on top, ending at `9a46847` — **NOT yet pushed**, this is local-only). Wave 4 covered: boolean-flag CLI parsing bug, domain-relative CRYSTAL_CHARS + rune-count fix (same bug family as an earlier CI regression I found and fixed in `repoRootForDomain`), `hotam init-project` (a new resolver-requested one-call external-project bootstrap command), `schema_version` + forward-compat loader gate, and a full triage of the "41 closeable enforcement debt" complaint: 8 resolver-decided items from an earlier-session decision brief were resolved (reword-only claim corrections, one real ENFORCED implementation for the missing-graph.json calm-notice gap, a structural/prose split of `R-work-within-launch-dir` with a re-ported Go AST scanner, and a Canon-sourced one-RULE-per-topic crystal distillation via an `@fx` design consultation), plus a NEW triage document (`docs/reviews/2026-07-13-c1-roadmap-debt-triage.md`) explaining that the remaining 33 items are honestly-documented feature-blocked roadmap (ticket engine, attention core, sub-agent hierarchy, sensorium hooks, etc. — none of these features exist in the Go port yet), not neglected debt, per an `@fm` advisory consultation that concluded force-closing them via fake tests or dishonest `INHERENTLY_PROSE` reclassification would violate the project's own honesty norms.

A 5th independent review (score 8.0/10, up from 7.9) was just pasted and analyzed. I independently verified its 5 P1 claims live (confirmed all real: a genuine second hand-maintained tool-registry table with real drift, a live-reproduced `ProjectRootUnresolved` failure for `hotam init <bare-dir>` → `gen-spec --domain <bare-dir>` outside a project, the already-known `hotam init-project` bare-command-without-`--domain` limitation, a stale README command count, and `what-now` still surfacing all 37 closeable-debt items as P0 despite the C1 triage doc explaining 33 are feature-blocked). The user then asked me to also think about how to reduce agent friction more broadly ("проще развивать/поддерживать/понимать требования, быстрее ловить нестыковки старого и нового, меньше прогонов") — I ran an `@fx` (max-effort) design consultation which produced a ranked list of concrete agent-UX improvements, headlined by a new `hotam propose` command (writes valid proposal JSON + runs an automatic `confront` check before writing, collapsing draft→confront→land from 3 round-trips into 1) as the single highest-leverage item, plus `confront`-at-the-land-gate (warn-only), a `hotam brief <id>` aggregator, extending `confront` with inspect's structural signals (shared-assumption/axis-co-reference) instead of lexical-only matching, `--json` coverage gaps, and a crystal command-reference trim. Embeddings-based semantic conflict detection and a separate `agent-brief` boot command were both explicitly evaluated and REJECTED (breaks stdlib-only/determinism; adds another source-of-truth surface, against the review's own thesis).

A full plan was written to `docs/reviews/2026-07-13-review5-response-plan.md` and decomposed into 13 strictly-sequential TaskList items (`#118`–`#130`), covering: killing the duplicate tool-registry table, fixing bare-domain `gen-spec`, active-domain resolution via a `.hotam-spec-project` marker + new `hotam use` command, README/CLI sync with a drift test, a structural test closing the `boolFlagNames`-sync gap (found by the wave-4 final review), the big one — splitting the one-dimensional burn-down metric into closeable-now vs feature-blocked (an ontology `blocked_on` field + generator + diagnose change, deferred twice before, now resolver-approved), then the 5 agent-UX items from the `@fx` consultation in dependency order, then a `--profile consumer|full` gen-spec flag to cut external-seed bloat (87 files → far fewer), and finally a docs/reviews+checkpoints archive index. **No implementation has started yet on this wave — all 13 tasks are `pending`, none `in_progress`.** No babysit cron is currently armed (the wave-4 one self-deleted when its TaskList emptied after wave 4 completed; a new one has not been armed yet for wave 5 since implementation hasn't been kicked off).

## Active goal

None — no `/goal` Stop hook is in force this session.

## TaskList

### pending (all 13, strictly chained via blockedBy, none started)
- #118 R5-a P1: убить дубль toolRequirementData — единый реестр tools (no blockers, ready to start)
- #119 R5-b P1: gen-spec для bare-domain — domain-dir как минимальный root (blockedBy: #118)
- #120 R5-c P1: active domain — маркер/env/hotam use вместо hardcoded default (blockedBy: #119)
- #121 R5-d P1: README ↔ CLI синхрон + drift-тест (blockedBy: #120)
- #122 R5-f: структурный тест синхронизации boolFlagNames (blockedBy: #121)
- #123 R5-e P1: сплит burn-down метрики — closeable-now vs feature-blocked (blockedBy: #122)
- #124 R5-i: hotam propose — генератор proposal JSON со встроенным confront (blockedBy: #123)
- #125 R5-j: confront на воротах land/apply-proposal, warn не fail (blockedBy: #124)
- #126 R5-k: hotam brief <id> — один вызов вместо show+context+related+due (blockedBy: #125)
- #127 R5-l: структурный confront — --proposal с inspect-сигналами (blockedBy: #126)
- #128 R5-m: --json везде + сокращение справочника команд в кристалле (blockedBy: #127)
- #129 R5-g: gen-spec --profile consumer|full (blockedBy: #128)
- #130 R5-h: индекс-архив для docs/reviews + docs/checkpoints (blockedBy: #129, last task)

### recently completed (wave 4, all committed 66e4af2..9a46847, NOT yet pushed)
- #117 C2-E: триаж-документ для 33 C1-пунктов (docs/reviews/2026-07-13-c1-roadmap-debt-triage.md)
- #116 C2-D: R-operator-crystal-embeds-thinking-distilled — Canon-based дистилляция
- #115 C2-C: R-work-within-launch-dir — расщепление на AST-сканер + prose
- #114 C2-B: R-empty-content-gen-notice — missing-file calm notice
- #113 C2-A: 4 reword-only C2-decision proposals
- #112 hotam init-project — полный бутстрап бизнес-проекта
- #110 P2 #4: schema_version в graph.json + толерантный loader
- #109 P2 #3: сократить seed-вывод (tools/INDEX.md)
- #108 P1 #2: domain-relative CRYSTAL_CHARS + rune count + sweep
- #107 P1 #1: boolean-флаги в reorderFlagsFirst

## Decisions

- Chose to run this wave's design work through TWO consultations rather than deciding unilaterally: `@fx` (max effort) for the "reduce agent friction" open question (per `R-ai-presents-not-decides` spirit — even though this is an engineering-tradeoffs question rather than a graph-content decision, the resolver explicitly asked me to "think about" it, which I read as wanting a considered recommendation, not a unilateral implementation).
- Explicitly rejected two agent-UX ideas from the consultation: embeddings-based semantic conflict detection (breaks stdlib-only + determinism, and the review itself says noise-reduction should come before smarter detection) and a separate `agent-brief` boot command (would be yet another source-of-truth surface, directly contradicting review 5's own central thesis about reducing sources of truth).
- Sequenced the burn-down-metric split (`#123`, the biggest single item) AFTER the 4 other P1 source-of-truth/domain-UX fixes (`#118`-`#121`) but BEFORE the agent-UX track (`#124`-`#128`) — reasoning: the metric split touches ontology (a `schema_version` bump may be required per the graph.go bump-procedure doc comment — task #123's description explicitly tells the sub-agent to read and follow that documented procedure rather than assume), so it needs a settled foundation first, but the agent-UX commands (`propose`, `brief`, structural confront) are additive and don't depend on the metric split, so scheduling is about risk-isolation, not a hard technical dependency chain — the whole thing is nonetheless kept strictly serial (matching every prior wave's execution style) to avoid file-churn collisions between sub-agents touching overlapping generator/registry surfaces.
- Chose NOT to have task #118 (tool-registry unification) prescribe the exact mechanical shape (move fields onto `methodology.Tool` vs. generate `toolRequirementData` from the registry) — left as an implementation-time judgment call for the sub-agent, since I hadn't fully traced every consumer of the old table before delegating.

## Open questions

- None outstanding from the user — the plan was fully approved via the review-planning conversation and the `@fx` consultation's recommendations were accepted as the agent-UX track without further pushback. The only latent uncertainty is technical (task #123's schema_version-bump question, task #118's exact unification mechanism) and is explicitly delegated to the implementing sub-agent's judgment with guidance to read the relevant doc comments first, not a resolver-facing open question.
- Whether to push wave 4's now-completed but unpushed commit chain (`66e4af2..9a46847`) before or after wave 5 lands — not asked, not decided; the user has not requested a push since wave 4 finished, so it stays local per the standing "never push without explicit request" rule.

## Repo state

```
?? docs/reviews/2026-07-13-review5-response-plan.md
```

```
9a46847 docs: C1 roadmap-debt triage — feature-blocked PROSE is honest, not neglected (task #117, review-4 final)
675c15d feat: Canon-sourced one-RULE-per-topic crystal distillation (task #116, review-4 C2 item 5)
792819e feat: split R-work-within-launch-dir into structural (AST scanner) + prose halves (task #115, review-4 C2 item 6)
1063590 feat: gen-spec renders calm notice on missing graph.json (task #114, review-4 C2 item 2)
bc7d6f2 docs: 4 resolver-approved C2 decisions (task #113, review-4 P2)
```
