# Checkpoint — 2026-07-15 11:03 [framework: HotamSpec waves 10-12 + corpusCommonTokens fix]

Это чекпойнт **фреймворковой стороны** сессии (сам движок HotamSpec, Go).
Пилотная сборка PRAT-hotam — в отдельном файле `PRAT-hotam/docs/checkpoints/2026-07-15-1103-pilot.md`.
Связующий нарратив всей сессии — в `D:/ai_dev/prat/SESSION-OVERVIEW-2026-07-15.md`.

## Session summary

Продолжение очень длинной многоволновой сессии по закалке HotamSpec (Go-реализация методологии tension-graph). Волны 8-9 (задачи #144-154) были завершены и **запушены** ранее в этой сессии (диапазон `9a46847..87bd711`, 39 коммитов). После пуша прошли ещё три волны + один внеплановый фикс — все **локальные, не запушены** (`origin/master..HEAD` = 8 коммитов `1c6371d..5cbfe70`).

Установившийся паттерн всей сессии: пользователь вставляет независимое внешнее ревью (`/fl`, `/fm`, `/oh`), я самостоятельно верифицирую claims против живого репо, декомпозирую в план + TaskList с blockedBy-цепочками, по явной команде `/babygoal` реализую волну через суб-агентов (`/crush`, при исчерпании лимитов до 2026-07-16 — `@sh`), каждый суб-агент независимо перепроверяется мной (zero-trust): `go build/vet`, `gofmt -l`, `go test ./...` (plain, ambient TMP), `go test -race -count=1` (чистый TMP=`D:\ai_dev\_clean_tmp`), `all-violations` на обоих self-доменах, идемпотентность gen-spec, плюс ручное воспроизведение бага/фикса. Коммит после каждой подзадачи; push и `@fl`-ревью — только по явному запросу.

Волны 10-12 были направлены на **готовность к приземлению бизнес-методологии** (не на self-hosting как раньше). Затем эта готовность была реально использована: PRAT приземлён на HotamSpec (см. pilot-чекпойнт). Во время пилота найден и исправлен реальный баг фреймворка (`corpusCommonTokens`), который на self-графе `hotam-spec-self` (237 SETTLED) никогда не проявлялся, но был мёртв на малых графах.

## Что сделано (незапушенные коммиты, новейшие сверху)

- `5cbfe70` — **fix corpusCommonTokens**: `ceiling = 0.05 * len(settled)` был < 1 для любого корпуса из 8-19 SETTLED-требований → ЛЮБОЙ токен (n≥1) исключался как «общий» → фильтр математически глушил 100% словаря → семантический гейт (opposite-marker + topical-token из `IsBlockingHit`) не мог найти ни одного якоря для блокировки. Мёртв на `hotam-dev` (9 SETTLED) и на любом малом внешнем домене. Фикс: `MinCorpusSizeForFrequencyFilter` теперь `int(math.Ceil(1.0 / CorpusCommonTokenFraction))` = 20 (наименьший N, при котором ceiling достигает 1.0). `CorpusCommonTokenFraction` (0.05, калиброван на реальном 234-графе) не тронут. Найден при пилоте PRAT (домен `prat`, 13 SETTLED). Покрыт 2 регресс-тестами (лично проверил, что падают на старом guard=8).
- `3d64ef2` — wave-12 R12-b: `hotam init`/`init-project` получили флаг `--require-provenance` (композиция с `--profile full` без взаимного затирания manifest).
- `99c292e` — wave-12 R12-a: `hotam propose requirement` получил флаги `--last-reviewed-at`/`--review-after` (закрыл дыру: provenance-гейт требовал поля, которые CLI не мог заполнить).
- `11e5ec3` — wave-11 R11-b: opt-in provenance-гейт (`require_provenance` в manifest.json). Ключевое: `SimulateRequirementResult` симулирует post-merge результат mutate() (не сырое предложение), чтобы UPDATE, опускающий provenance-поля, корректно проходил (coalesce-preserve). Работает на всех 3 путях (land / apply-proposal / batch).
- `fbcf3ad` — wave-11 R11-a: seed-требование `R-domain-exists` больше не даёт ложный первый приоритет «никогда не проверялось» (init задаёт `last_reviewed_at`/`review_after` = today+180d).
- `5425af5` — регенерация корневого crystal + `hotam-spec-self/docs/gen/` (подхватил текст мета-языка из R10-b + накопившийся source-text долг).
- `a606512` — wave-10 R10-b: **мета-язык** — распознавание зарезервированных капс-токенов (ALWAYS/NEVER/MUST/MUST NOT/ONLY/ANY) независимо от языка прозы; агент вставляет их на шаге TRANSLATE. Плюс исправлен реальный substring-баг (`strings.Contains(claim,"any")` ложно срабатывал внутри «company»/«many»). Плюс `tokenRE` расширен с ASCII на Unicode `\p{L}\p{N}` (иначе кириллические claim'ы токенизировались в пустоту).
- `1c6371d` — wave-10 R10-a: batch-импорт (`--batch`) теперь тоже проходит семантический гейт (раньше обходил). Логика вынесена в `internal/diagnose.IsBlockingHit`; batch использует dependency injection (`ConflictChecker`), т.к. `internal/proposal` (core) не может импортировать `internal/diagnose` (periphery) — `R-core-periphery-import-ratchet`.

## Active goal

Активная Stop-hook цель на момент чекпойнта: «реализуй план, используй агентов @sh, заведи таски и выполняй, между тасками делай коммиты» (`/babygoal` для сборки PRAT-hotam). TaskList на момент чекпойнта ПУСТ — все 13 задач волны сборки закрыты, babysit-крон снят.

## TaskList
### recently completed (все закрыты, TaskList пуст)
- #155-160: волны 10-12 HotamSpec (см. коммиты выше)
- #173: фикс corpusCommonTokens
- #161-172: сборка PRAT-hotam (в pilot-чекпойнте)

## Decisions
- corpusCommonTokens: выбрана производная константа `ceil(1/CorpusCommonTokenFraction)`, а не жёсткий литерал 20 — чтобы две константы не рассинхронизировались при будущей перекалибровке fraction.
- provenance-гейт: сделан OPT-IN через manifest-флаг (не глобальное поведение) — `hotam-spec-self`/`hotam-dev` не затронуты по умолчанию.
- provenance-проверка: симулирует post-merge результат, а не сырое предложение — иначе content-only UPDATE ломался бы на coalesce-preserve семантике.
- Каждый гейт (semantic, provenance) покрывает ВСЕ 3 пути записи (single-file land, apply-proposal, batch) — не повторять дыру «single защищён, batch обходит».

## Open questions / долги
- **Push не сделан** — 8 коммитов (`1c6371d..5cbfe70`) локальны. Ждёт явной команды resolver.
- **ProposedStakeholder не поддерживает update-семантику** (в отличие от ProposedRequirement) — нельзя точечно исправить опечатку в заведённом stakeholder без пересоздания. Найдено при пилоте (этап 7). Кандидат в будущий фикс, не блокер.
- **`m_tag` зарезервирован** под M-decision registry (формат `M[1-9][0-9]*`, только OPEN-статус) — не годится для произвольной группировки. Найдено при пилоте (этап 8). Кандидат в отдельное поле-тег, если понадобится.

## Repo state
```
origin/master..HEAD (unpushed):
5cbfe70 fix: corpusCommonTokens no longer wipes all vocabulary for 8-19 SETTLED requirements
3d64ef2 feat: hotam init/init-project gain --require-provenance flag (task #160, wave-12 R12-b)
99c292e feat: propose requirement gains --last-reviewed-at/--review-after flags (task #159, wave-12 R12-a)
11e5ec3 feat: opt-in provenance gate for business-import SETTLED requirements (task #158, wave-11 R11-b)
fbcf3ad fix: seed requirement no longer trips a false first-priority freshness signal (task #157, wave-11 R11-a)
5425af5 docs: regenerate crystal + hotam-spec-self docs/gen/
a606512 feat: language-independent reserved-token markers (task #156, wave-10 R10-b)
1c6371d fix: batch imports now run the semantic-conflict gate too (task #155, wave-10 R10-a)
```
```
working tree — только неотслеживаемые bookkeeping-файлы:
?? docs/checkpoints/2026-07-14-0611.md
?? docs/checkpoints/2026-07-14-1716.md
?? docs/reviews/2026-07-14-review8-response-plan.md
(+ этот чекпойнт после записи)
```
