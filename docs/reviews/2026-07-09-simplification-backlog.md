<!-- LEGACY (Python-era) audit/review — describes a point-in-time snapshot of the Python prototype (pytest/spec/tools references below are historical, not current instructions); see README.md for the current Go CLI. -->

# Бэклог упрощения — задачи из пятилинзового ревью 2026-07-09

Источник: `2026-07-09-simplification-review.md` + `lens-1-redundancy.md` … `lens-5-agent-cognitive-load.md`.
Дедуплицировано: находки, повторяющиеся в нескольких линзах, слиты в одну задачу с перечислением источников.

Легенда полей:
- **[механика]** — не требует решения стьюарда по существу, только санкции на выполнение; делегируемо агенту.
- **[решение]** — меняет онтологию/правила/позиционирование; сначала PRESENT стьюарду, потом код (R-ai-presents-not-decides).
- Усилие: S (< полдня агента), M (волна), L (несколько волн).

---

## Группа A — Сигнал/шум attention (быстрые победы, самая высокая отдача на усилие)

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| A1 | Batch-миграция всех 38 REJECTED на structural `replaces`-рёбра (один batch-proposal). Закрывает 38 из 40 сигналов навсегда; после неё убрать прозо-парсер маркера REPLACES из gen_spec (данные переезжают в рёбра). | механика | M | L1#5, L2#1, L5#1 |
| A2 | Advisory-band: «Advisory; never a gate» не может жить в P0. Либо новый band (P7 ADVISORY), либо отдельное поле severity. P0 снова означает «брось всё». | решение (лёгкое) | S | L5#2 |
| A3 | Агрегация повторяющихся сигналов одного типа в одну строку-счётчик (прецедент уже есть: P5 latent-connectors рендерятся кластерами). Защита от будущих «стен из 38 строк». | механика | S | L5#1 |

## Группа B — Вывод из эксплуатации отвергнутых/мёртвых механизмов

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| B1 | Удалить `record_delegation.py` + `delegations.jsonl` (устаревшая система делегаций): мигрировать trust-anchor-записи (DEL-1) в frontmatter DG-файлов или HISTORY, снести ~460 строк кода+тестов+CLI-обёртку. R-trust-anchor-* атомы обновить через петлю. | решение | M | L1#1 |
| B2 | Entity/federation-подсистемы → «attic»: отдельный pytest-маркер, вне T1 и вне run-speed baseline. Либо (жёстче) удалить Entity-слой целиком до первого реального потребителя — git всё хранит. ~1000+ строк из горячего пути. | решение | M | L1#3, L2#1 |
| B3 | Удалить/переписать `docs/demo/` — устаревший артефакт (имя «Tensio», мёртвые пути `spec/content/graph.py`), вводит новичка в заблуждение. | механика | S | L3#5 |
| B4 | `tick.py` → слить в `what_now --report` (он лишь обёртка над `what_now.diagnose` + рендер). Минус один инструмент из 35. | механика | S | L1#5 |
| B5 | `_comment`-история разморозок (~2.5k символов) из `frozen_aspects_baseline.json` → в HISTORY.md/git; в JSON — только последняя запись. | механика | S | L1#2 |

## Группа C — Консолидация дублирующихся механизмов

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| C1 | Три baseline-механизма → один `protected_baselines.json` с секциями `{name: {rule, files{path:hash}}}` + одна параметризованная логика рехеша/проверки в update_baseline.py + один параметризованный тест. | механика | M | L1#2 |
| C2 | Пульс-конвейер: `emit_cipher.py` перестаёт regex-парсить собственный сгенерированный markdown — зовёт `attention.collect`/`diagnose` напрямую, либо gen_spec пишет машиночитаемый `live-state.json` рядом с CLAUDE.md. | механика | M | L1#5 |
| C3 | `gate.py`/`gate_status.py`/`closure.py` → один `land.py` с сабкомандами (три CLI вокруг одного land-log.jsonl). | механика | M | L1#9 |
| C4 | 31 файл `cli/` → генерация entry-points из таблицы + один модуль-диспетчер. Также чинит рассинхрон имён (`cli/mark_revisit` vs `tools/mark_revisit_evaluated`). | механика | S | L1#4 |

## Группа D — Упрощение онтологии (все требуют решения стьюарда)

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| D1 | Goal/Process/EntityType/Operator (суммарно 4 инстанса на 291 узел) → «extension types»: вынести из ядра кристалла/Concept Map/Proposed*-перечня до появления реального потребителя. Goal, возможно, слить с Requirement (GOAL-префикс уже существует). | решение | L | L2#1 |
| D2 | `supports` + `refines` → одно ребро (ни один check их не различает). | решение | S | L2#1 |
| D3 | Enforcement-градиент: PROSE/STRUCTURAL/ENFORCED → бинарный ENFORCED/NOT_ENFORCED + флаг `inherently_prose`. STRUCTURAL-узлы честно становятся NOT_ENFORCED-долгом (усиливает ratchet). | решение | M | L2#2 |
| D4 | `"OPEN(<question>)"` в строке статуса → отдельное поле `question: str` (сейчас парсится регексом из статуса). | механика | S | L2#2 |
| D5 | Кусты правил одного механизма (gate — 7 атомов, attention — 4, tension-audit — 3, active-loop — 3) → правило-кластеры (родительский атом + дети как enforced_by-строки). Индекс кристалла ~110 → ~40 строк без потери enforcement. | решение | M | L2#3 |

## Группа E — Кристалл-диета 2.0 (сигнал, не символы)

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| E1 | Constitution index: резидентными остаются только [P]/[S]-правила (~25 — их держит только внимание агента); [E]-правила (~85, и так пойманы gate) — одна строка-ссылка на docs/gen/CONSTITUTION.md. | решение (лёгкое) | S | L5#3, L2#5 |
| E2 | Tool reference → группировка по задаче («Пульс: …», «LAND: …», «Тикеты: …», «Скаффолдинг: …») вместо 35 алфавитных однострочников; редкие инструменты — только в docs. | механика | S | L5#4 |
| E3 | Methodology-секция: машинно-обрезанные первые предложения докстрингов → рукописные однопредложенные summary (механизм summary-поля уже есть — R-crystal-carries-short-form; применить к thinking-докам). | механика | M | L5#6 |
| E4 | Радикальная (обсуждаемая) версия: кристалл = закон + петля + 4 команды + LIVE-STATE (~5k chars); всё справочное — по ссылке. | решение | M | L2#5 |

## Группа F — Мед.петля: честность декларации

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| F1 | Узаконить tier-ованную петлю: полные 6 шагов — для изменений графа (там они и так принуждены инструментами); для read-only запросов — ORIENT + ответ с якорями. Снимает разрыв декларации и практики (прецедент: R-boot-cite-measured → R-agent-conduct-is-rules-not-tests). | решение | S | L5#5 |

## Группа G — Consumer-документация и доделка портативности

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| G1 | QUICKSTART-CONSUMER.md: путь `pip install hotam-spec` → `hotam-create-domain my-shop --activate` → `hotam-what-now` в чужом репо; объяснение project-root резолвинга. Пробел №1 портативности. | механика | M | L3#1 |
| G2 | **R6-fallback опасность**: консюмер без маркеров получает не ошибку, а тихо неправильный корень (данные пишутся в каталог установки фреймворка). Требуется: в consumer-режиме (фреймворк установлен как пакет, не self-hosting) R6 должен падать с диагностикой, а не угадывать. | решение | M | L3#3 |
| G3 | Реальный e2e-тест «с улицы»: pip install в чистый venv, пустой чужой каталог, прогон quickstart (не pytest tmp_path — настоящий subprocess-сценарий). | механика | M | L3#2 |
| G4 | Сузить R3-маркеры: `CLAUDE.md` и `tickets/` — слишком общие (любой Claude-Code-репозиторий ложно распознается как hotam-проект). | решение | S | L3#7 |
| G5 | Справочник Proposal JSON для человека (одна страница: kinds, обязательные поля, примеры) — сейчас единственный источник — исходник proposal.py. | механика | S | L3#3 |
| G6 | В README/Adopt разделить шаги: «команда без Claude» vs «команда с AI-оператором» (hooks/sensorium — опциональный AI-слой, не обязательный шаг 2). | механика | S | L3#4 |
| G7 | Глоссарий 6 стартовых терминов в README (Stakeholder, Requirement, Axis, Conflict, steward≠owner, Assumption) + граница «operators/crystals/budgets нужны только для AI-оператора». Заменить захардкоженный бейдж «944 passing». | механика | S | L3#6, L3#7 |

## Группа H — Тестовая гигиена

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| H1 | Удалить sys.path-бойлерплейт из 92 тест-файлов (conftest.py уже делает то же самое) — ~550 строк в ноль. | механика | S | L1#6 |
| H2 | Параметризовать семейства клонов: `test_tool_ticket_{create,comment,edit,move}`, `test_tool_create_{agent,axis,domain,entity_type}`, `test_tool_apply_proposal_{assumption,stakeholder}`. | механика | M | L1#6 |

## Группа I — Архитектурные (большие, отдельные волны)

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| I1 | gen_spec: перевести контракт тестов с байтов markdown на промежуточную модель блоков (list of typed blocks; рендер — тонкий слой). Это НЕДОСТАЮЩЕЕ УСЛОВИЕ безопасного распила монолита (подтверждено отказом волны 6.2b). Заодно убрать `--help`-сабпроцессы. | решение | L | L2#4, L1#8 |
| I2 | apply_proposal.py (3555 строк) — распил писателей по типу proposal (после I1 или независимо). | механика | L | L1#8 |
| I3 | docs/gen консолидация: 5 проекций графа → REQUIREMENTS + TENSIONS + HISTORY; CONSTITUTION/FRAMEWORK-INVARIANTS/UNENFORCED — секции одного файла. | решение | M | L1#7 |

## Группа J — Стратегические (только стьюард, не делегируется)

| ID | Задача | Тип | Усилие | Источник |
|----|--------|-----|--------|----------|
| J1 | **Позиционирование**: заявлено «методология противоречивых бизнес-требований», подтверждённая ниша — «память и дисциплина для 1-3 человек + флот LLM-агентов». Решить: перепозиционировать честно, или доказать заявленное (см. J2). | решение | — | L4 |
| J2 | **Первый реальный внешний домен** (prat или другой) — единственный способ проверить гипотезу переносимости онтологии. До него применимость — UNCERTAIN-допущение, на котором висит весь проект. Кандидат в Assumption-узел графа (A-ontology-transfers). | решение | L | L4 |
| J3 | **271:8** — Requirement:Conflict. Центральная механика (граф противоречий) используется в 34 раза реже реестра правил. Вопрос стьюарду: это болезнь роста self-hosting-периода или сигнал, что ценность — в реестре, а конфликты — периферия? Влияет на J1. | решение | — | L4 |

---

## Рекомендуемый порядок (координатор, не решение)

1. **Сессия быстрых побед** (механика, S): A1+A3 (шум), B3, B4, B5, C4, D4, H1 — суммарно ~1-2 волны, репозиторий заметно чище.
2. **Консолидация** (механика, M): C1, C2, C3, H2, G1+G5+G6+G7 (consumer-доки пакетом).
3. **Решения стьюарда по одному** (как прошлые подпись-сессии): A2, B1, B2, D1, D2, D3, D5, E1, E4, F1, G2, G4, I3 — каждый вопрос отдельно, просто, «будто с нуля».
4. **Большие волны** (после решений): I1 → I2, D1-исполнение, J2 (реальный домен).
5. **Стратегия** (J1, J3) — разговор, не волна.

Статус: записано 2026-07-09, задачи НЕ заведены в TaskList и НЕ начаты — ждёт триажа стьюарда.
