<!-- AUTOGENERATED from internal/generator + domain graph.json — do not edit by hand. Edits: methodology/graph -> go run ./cmd/hotam gen-spec -->

# §Entity

## Canon

Domain-declared business concept with its own lifecycle (M12 opt-in aspect): EntityType + EntityField + EntityInstance.

## Narrative

A domain (e.g. a finance app) declares EntityType('customer', lifecycle=…, fields=…) in its build_graph(); the framework accepts it as first-class and the check_entity_* family covers every declared type by iteration. No new code per entity — coverage is generated by iterating g.entity_types and g.entities. It reuses the §Lifecycle keystone: EntityType.lifecycle is a Lifecycle, validated by check_lifecycle_wellformed — no parallel state machinery for entity states.

## Why

WHY a declarative aspect (not framework code per entity): domain-declared entities keep the framework content-free while letting each domain model its own business concepts with real state machines. Iterating declared types for coverage means a new EntityType needs no new check code — the invariant surface scales with the domain's declarations, not with framework edits.
