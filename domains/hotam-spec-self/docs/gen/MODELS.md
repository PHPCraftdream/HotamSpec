<!-- AUTOGENERATED from the domain graph.json — do not edit by hand. Edits: methodology/graph -> hotam gen-spec -->
reader: domain-user

# MODELS.md — authored object model overview (Hotam-Spec)

Generated by parsing this domain's authored `spec/` tree with `go/ast` (PLAN-authored-spec-discipline.md §7): objects (structs/types), their fields, their methods, and their typed sentinel errors, in general -> specific order. Read-only overview -- it does not execute or type-check the source, only inventories declarations.

**13 file(s) scanned; 13 object(s); 0 typed error(s).**

---

## `internal/gate/spec_resolver.go`

package `gate`

### `SpecSymbolKind` (type)

SpecSymbolKind classifies what an implemented_by entry resolved to.

### `SpecSymbolResult` (struct)

SpecSymbolResult is the outcome of resolving one implemented_by entry (file:symbol) against a single named file in a domain's authored spec/ tree.

**Fields:**

| field | type |
|---|---|
| `Kind` | `SpecSymbolKind` |

**Methods:**

- `func (r SpecSymbolResult) Found() bool`
  Found reports whether the symbol was located.

### `SpecTestResult` (struct)

SpecTestResult is the outcome of resolving one verified_by entry (file:test) against a single named file in a domain's authored spec/ tree.

**Fields:**

| field | type |
|---|---|
| `Found` | `bool` |
| `HasTeeth` | `bool` |
| `HasSkip` | `bool` |

## `internal/gate/spec_resolver_test.go`

package `gate`

_No exported objects or typed errors in this file._

## `internal/generator/constitution_entities_framework_repomap_test.go`

package `generator`

_No exported objects or typed errors in this file._

## `internal/generator/traceability.go`

package `generator`

_No exported objects or typed errors in this file._

## `internal/invariants/authored_links.go`

package `invariants`

_No exported objects or typed errors in this file._

## `internal/invariants/authored_links_test.go`

package `invariants`

_No exported objects or typed errors in this file._

## `internal/loader/lock_real_domains_test.go`

package `loader`

_No exported objects or typed errors in this file._

## `internal/ontology/conflict.go`

package `ontology`

### `Conflict` (struct)

**Fields:**

| field | type |
|---|---|
| `ID` | `string` |
| `Axis` | `string` |
| `Context` | `string` |
| `Members` | `[]string` |
| `Steward` | `string` |
| `Lifecycle` | `string` |
| `SharedAssumption` | `*string` |
| `Derived` | `[]string` |
| `RevisitMarker` | `string` |
| `DecidedBy` | `string` |
| `Variants` | `[]Variant` |
| `Signoff` | `*Signoff` |
| `CreatedAt` | `string` |
| `DecidedAt` | `string` |
| `DeclOrder` | `int` |

**Methods:**

- `func (c Conflict) IsDecided() bool`
- `func (c Conflict) IsHeld() bool`
- `func (c Conflict) IsUnresolved() bool`

### `Variant` (struct)

**Fields:**

| field | type |
|---|---|
| `ID` | `string` |
| `Behavior` | `string` |
| `Implies` | `string` |
| `Costs` | `string` |

## `internal/ontology/graph_smoke_test.go`

package `ontology`

_No exported objects or typed errors in this file._

## `internal/ontology/lifecycle.go`

package `ontology`

### `Lifecycle` (struct)

**Fields:**

| field | type |
|---|---|
| `Slug` | `string` |
| `States` | `[]State` |
| `Transitions` | `[]Transition` |
| `Cyclic` | `bool` |
| `PrefixStates` | `[]string` |

**Methods:**

- `func (l Lifecycle) CanTransition(fromState, toState string) bool`
- `func (l Lifecycle) Initial() (State, error)`
- `func (l Lifecycle) Matches(value string) (State, bool)`
- `func (l Lifecycle) StateNames() map[string]struct{}`
- `func (l Lifecycle) TransitionFor(fromState, event string) (Transition, bool)`
- `func (l Lifecycle) prefixSet() map[string]struct{}`

### `State` (struct)

**Fields:**

| field | type |
|---|---|
| `Name` | `string` |
| `Kind` | `string` |
| `Why` | `string` |

**Methods:**

- `func (s State) IsInitial() bool`
- `func (s State) IsTerminal() bool`

### `Transition` (struct)

**Fields:**

| field | type |
|---|---|
| `Src` | `string` |
| `Dst` | `string` |
| `Event` | `string` |
| `Guard` | `string` |
| `GuardAssumption` | `*string` |
| `Why` | `string` |

## `internal/ontology/requirement.go`

package `ontology`

### `HistoryEntry` (struct)

**Fields:**

| field | type |
|---|---|
| `At` | `string` |
| `Summary` | `string` |
| `DecidedBy` | `string` |

### `Relation` (struct)

**Fields:**

| field | type |
|---|---|
| `Kind` | `string` |
| `Target` | `string` |

### `Requirement` (struct)

**Fields:**

| field | type |
|---|---|
| `ID` | `string` |
| `Claim` | `string` |
| `Owner` | `string` |
| `Status` | `string` |
| `Why` | `string` |
| `Assumptions` | `[]string` |
| `Relations` | `[]Relation` |
| `Enforcement` | `string` |
| `EnforcedBy` | `[]string` |
| `MTag` | `string` |
| `Enforceability` | `string` |
| `Summary` | `string` |
| `CreatedAt` | `string` |
| `SettledAt` | `string` |
| `LastReviewedAt` | `string` |
| `ReviewAfter` | `string` |
| `Evidence` | `[]string` |
| `SourceRefs` | `[]string` |
| `History` | `[]HistoryEntry` |
| `DeclOrder` | `int` |
| `BlockedOn` | `string` |
| `ImplementedBy` | `[]string` |
| `VerifiedBy` | `[]string` |

**Methods:**

- `func (r Requirement) IsCloseableDebt() bool`
- `func (r Requirement) IsCloseableDebtNow() bool`
  IsCloseableDebtNow is the actionable subset of closeable debt: a real test could be written for it TODAY if someone did the work (no missing feature blocks it). Mutually exclusive with IsFeatureBlockedDebt; their union equals IsCloseableDebt.
- `func (r Requirement) IsFeatureBlockedDebt() bool`
  IsFeatureBlockedDebt is the honestly-documented roadmap subset of closeable debt: the requirement describes a feature that does not exist yet, so no real enforcement test is possible until the blocking feature is built. See R-speculative-aspects-frozen.
- `func (r Requirement) IsOpen() bool`

## `internal/proposal/apply.go`

package `proposal`

### `ConflictChecker` (type)

ConflictChecker is injected by a periphery-aware caller (cmd/hotam) so internal/proposal (core) never imports internal/diagnose (periphery) -- see R-core-periphery-import-ratchet, enforced by internal/selfcheck/imports_test.go's TestCorePeriphery_ImportRatchet. It returns a non-nil, ready-to-surface error if claim semantically conflicts with SETTLED content in g, or nil if clear.

### `ProvenanceChecker` (type)

ProvenanceChecker is injected by the caller (cmd/hotam) so ApplyBatch can enforce the opt-in provenance gate (require_provenance in manifest.json — see internal/loader.ResolveRequireProvenance and cmd/hotam/provenance_gate.go) on the batch path with the SAME logic the single-file path (cmd/hotam's provenanceGate) runs, closing the class of "single-file protected, batch bypassed" gap task #155 closed for ConflictChecker. It is called for each ProposedRequirement BEFORE applyToGraph mutates g, against the ROLLING in-memory graph (so it reflects proposals already applied earlier in the same batch), and returns a non-nil, ready-to-surface error naming the missing provenance field(s), or nil if the requirement's simulated post-merge result satisfies the gate (or the gate is not opted into for this domain). checkProvenance may be nil, in which case no provenance checking is performed (used by callers/tests that don't need the gate).

## `internal/proposal/proposal_test.go`

package `proposal`

_No exported objects or typed errors in this file._
