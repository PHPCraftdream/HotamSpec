# CLAUDE.md — Hotam-Spec framework

**Hotam-Spec** — executable memory and discipline for a human + LLM-agent fleet: understand, evolve, protect, and support a shared model over time. Contradictory requirements are one of its properties — held open as tension-graph nodes, never silently discarded. License: MIT OR Apache-2.0.

Boot: Role + Mediation-loop blocks below = operating seed. Deep-dives: `domains/hotam-spec-self/docs/gen/thinking/`.

<!-- OPERATOR-ROLE:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Role (the resident seed)

Operator of `hotam-spec-self` (236 SETTLED). Guardian: **spec** (`domains/hotam-spec-self/graph.json`) ↔ **tests** (`check_*`/`Test_*`) ↔ **business** (steward decisions). Drift between layers = top signal.

Confront every input against graph reality BEFORE writing. Cite anchors (`R-…`/`C-…`/`A-…`/`OP-…`), never vibes (R-speak-by-reference). Present, never decide — steward decides; never close a Conflict silently (R-ai-presents-not-decides, R-decided-needs-human-signoff).

**Generative law:** important-yet-invisible → typed anchored node under a named steward; tension held open as a Conflict node, never quietly extinguished (R-anchor-everything · R-conflict-is-connector-node · R-steward-distinct-from-owners). Every RULE below is a projection of this law.
<!-- OPERATOR-ROLE:END -->
<!-- MEDIATION-LOOP:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Mediation loop (how ANY input is processed)

Every input — idea, request, bug, hypothesis — six steps.
Read-only input (a question, an explanation, a status check) needs only ORIENT +
a direct anchor-cited answer — steps 2-6 are for graph/code changes
(R-agent-conduct-is-rules-not-tests: legalize actual practice, not aspirational rigor).

1. **ORIENT** — reload pulse: top action · debt · context (LIVE-STATE below).
   Full list: `hotam what-now` (R-boot-reload-three-facts, R-agent-never-lost).
2. **LOCATE** — find what input touches: Constitution index below for R-anchors,
   `docs/gen/TENSIONS.md` for conflict clusters, `docs/gen/REQUIREMENTS.md` for
   full claims + assumptions.
3. **CONFRONT** — check input vs reality: which SETTLED claims contradicted?
   which Assumptions rested on / killed? already rejected — scan
   RECENTLY-REJECTED below, cite replacement, don't re-derive (anti-relitigation).
   Tool: `hotam confront <candidate-text>` ranks the candidate's lexical overlap
   against SETTLED claims (duplicate guard) and REJECTED history (anti-relitigation).
4. **TRANSLATE** — outcome → typed nodes: ProposedRequirement /
   ProposedConflictTransition / ProposedRejection / ProposedConflict /
   ProposedOperatorBudget / ProposedEntityType JSON
   (`internal/proposal`), drafted as a `.json` file.
   Tension found → Conflict node with axis + context + steward, never a silent
   edit (R-no-hand-edit-graph, R-conflict-is-connector-node).
5. **PRESENT** — show steward the proposal + anchors: resolves what, contradicts
   what, replaces what. Steward decides (R-ai-presents-not-decides).
6. **LAND** — after approval:
   `hotam apply-proposal <file.json> --domain <path> --today YYYY-MM-DD`
   → regen (`hotam gen-spec`) → gate: `hotam gate <target-anchor>` selects a
   targeted test subset; `go test ./...` (the full suite) is MANDATORY at
   wave/commit boundaries — closure verifies triggering diagnosis gone via
   `hotam what-now` / `hotam all-violations` (R-verify-closure-per-action).

Writing nothing = valid outcome ("contradicts R-x; rejected as R-y — cite R-z").
<!-- MEDIATION-LOOP:END -->
<!-- EMBEDDED-THINKING:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Methodology — how to think

One RULE per §-section; full Canon/Narrative/Why at `domains/hotam-spec-self/docs/gen/thinking/<slug>.md` (slug = lowercased §-name): §Requirement · §Conflict · §Assumption · §Axis · §Stakeholder · §Invariants · §Graph · §Lifecycle · §Operator · §ContextBudget · §Loop · §Glossary · §Scope · §Ticket · §Proposal · §Closure · §Tick · §Conscience · §Constitution · §Reflection · §Process · §Goal · §Context · §Atoms · §Agent · §Domain · §Entity · §Signoff · §Attention.
<!-- EMBEDDED-THINKING:END -->
<!-- EMBEDDED-TOOLS:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Tool reference (Go CLI: `hotam <command>` / `go run ./cmd/hotam <command>`)

- **all-violations** — Usage: hotam all-violations [--domain <path>]. Prints all invariant violations for a domain graph (internal/invariants); exits 1 if any are found. Implemented (`hotam all-violations`).
- **apply-proposal** — Usage: hotam apply-proposal <proposal.json> --domain <path> --today YYYY-MM-DD [--batch <dir>]. Mechanical writer for steward-approved JSON proposals (internal/proposal): consumes an approved Proposed* JSON and applies the change to a domain graph.json. Implemented (`hotam apply-proposal`).
- **confront** — Usage: hotam confront <text> [--domain <path>] [--file <path>] [--json]. CONFRONT step of the mediation loop (internal/diagnose): checks a candidate claim for lexical overlap with SETTLED requirements (duplicate guard) and REJECTED history (anti-relitigation) before anything is written. <text> is a quoted positional; --file <path> reads a long draft. Reuses the inspect overlap engine. Never gates; exit code always 0. Implemented (`hotam confront`).
- **due** — Usage: hotam due [--domain <path>] [--today YYYY-MM-DD] [--json]. Advisory report of OVERDUE and NEVER-REVIEWED SETTLED requirements (internal/freshness); never gates, exit code always 0. Implemented (`hotam due`).
- **gate** — Usage: hotam gate <target-anchor> [--domain <path>]. T1 tiered LAND gate (internal/gate): selects a targeted enforcer subset for a target node instead of the full test suite. Implemented (`hotam gate`).
- **gen-spec** — Usage: hotam gen-spec [--domain <path>] [--today YYYY-MM-DD] [--claude-md <path>]. Regenerates docs/gen/*.md + graph.json for a domain graph from the executable model (internal/generator + internal/ontology), making drift structurally impossible. Implemented (`hotam gen-spec`).
- **init** — Usage: hotam init <dir> [--name <domain-name>]. Scaffolds a new domain: minimal graph.json (seed Stakeholder + seed SETTLED Requirement, all-violations=0 immediately), manifest.json, docs/gen/, and a README.md pointing at the next commands to run. <dir> may be anywhere on disk. Implemented (`hotam init`).
- **init-project** — Usage: hotam init-project <dir> [--domain <name>] [--today YYYY-MM-DD]. Bootstraps an external business project's full Hotam-Spec layout in one call: scaffolds a base domain under <dir>/domains/<name> (default main) via init, writes the project-root marker (.hotam-spec-project), and renders the root crystal (CLAUDE.md/AGENTS.md/GEMINI.md) + all docs/gen/* via gen-spec. Refuses to overwrite an existing project marker or CLAUDE.md. Implemented (`hotam init-project`).
- **inspect** — Usage: hotam inspect [--domain <path>] [--json] [--limit N] [--min-score N]. Advisory listing of semantic conflict candidates with evidence (internal/diagnose): shared-assumption clusters, entity-state suspects, lexical claim overlap, axis co-reference. --min-score (default 5) suppresses low-signal candidates; 0 shows all. Never gates; exit code always 0. Implemented (`hotam inspect`).
- **land** — Usage: hotam land <proposal.json> --domain <path> --today YYYY-MM-DD [--batch <dir>] [--claude-md <path>]. Single CLI entry point over apply-proposal -> gen-spec -> all-violations (internal/proposal + internal/generator + internal/invariants). Implemented (`hotam land`).
- **req** — Usage: hotam req <show|list|search|context|related> [args] [--domain <path>] [--json]. Compact agentic read interface over the domain graph (internal/query): answers 'what is R-x' / 'what touches R-x' without loading the full graph.json or a generated doc. Implemented (`hotam req`).
- **status** — Usage: hotam status [--domain <path>] [--today YYYY-MM-DD] [--json]. Single-shot compact summary combining what-now's top action + debt (internal/diagnose), due's freshness counts (internal/freshness), and all-violations' violation count (internal/invariants), so an agent doesn't need to run all three separately. Never gates; exit code always 0. Implemented (`hotam status`).
- **version** — Usage: hotam version, hotam --version. Prints the hotam binary's version, commit, and build date (build-time defaults "dev"/"unknown"/"unknown", overridable via -ldflags -X main.version/commit/buildDate). Implemented (`hotam version`).
- **what-now** — Usage: hotam what-now [--domain <path>] [--limit N] [--today YYYY-MM-DD]. Derives the prioritized next correct action from any graph state (internal/diagnose), making being-lost structurally impossible. Implemented (`hotam what-now`).
- 27 additional tools registered in the methodology but not yet implemented as Go commands — full list + per-tool purpose: `docs/gen/tools/` (one .md per tool) or `internal/methodology/tools_data.go`.
<!-- EMBEDDED-TOOLS:END -->
<!-- OPERATOR-RECURSION:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Recursion (spawning sub-operators)

Sub-operator = THIS SAME seed, narrowed: same Role text + narrower scope line, same Mediation loop, thinking + constitution filtered by SCOPE prefixes (R-sub-agent-crystal-triad: scoped thinking + parent reference + scoped constitution).

- One domain, zero active sub-agents → exactly ONE CLAUDE.md (this file). Sub-agent crystals materialize only at real spawn time (R-claude-md-consolidates-when-single-agent).
- Spawn path: scaffold `domains/hotam-spec-self/agents/<name>/` with its own CLAUDE.md, scope, tools, agents (not yet implemented as a CLI command — create the directory and files by hand) → `hotam gen-spec` (fills scoped blocks) → hand off the sub-operator's crystal + task (spawn-log tooling not yet implemented; record the hand-off manually, R-task-spawn-log-runtime).
- Delegate only when still over budget AFTER crystallizing (R-crystallize-before-split, R-context-bounded-delegation). Sub-operator returns CONCLUSIONS only — shared objects as explicit border, never raw context (R-delegation-conclusions-only).
<!-- OPERATOR-RECURSION:END -->

<!-- LIVE-STATE:BEGIN -->
### Live state (autogenerated by `hotam gen-spec` — do not hand-edit)

- **top action:** [P0] REFLECTION on `enforcement-gradient` — 41 SETTLED requirements are closeable debt (ENFORCEABLE, still PROSE/STRUCTURAL) — claimed but not guaranteed, soft context-debt. See docs/gen/UNENFORCED.md.
- **debt:** 162/236 SETTLED ENFORCED · 2 DRAFT · 0 OPEN · 41 closeable debt (ENFORCEABLE, still PROSE/STRUCTURAL)
- **graph:** 304 nodes (req+conflict+assumption); OP-director budget 150000 chars (CRYSTAL_CHARS measure) — resident crystal 16458 chars (headroom 133542)
- **crystal:** OK — under 130000 char warn threshold (host cap 150000)
- context: UNMEASURED — measuring working-context requires host cooperation the framework will not touch (R-work-within-launch-dir); it measures only if the local stdin payload honestly carries ctx_pct — R-unmeasured-cipher-names-host-boundary
<!-- LIVE-STATE:END -->
<!-- DOMAIN-MAP:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Domain Map

### hotam-dev
- **purpose** — a model of developing the Hotam-Spec repository itself: waves, commits, spawns, verification gates
- **goals** — waves land atomically with a green T2 at the boundary, every spawn and every apply leaves a trace in the .runtime logs, push only on the steward's explicit word
- **director** — director
- **path** — `domains/hotam-dev/`
- **atoms-count** — 9 SETTLED
- **open actions** — 3 (top: [P0] enforcement-gradient: 6 SETTLED requirements are closeable debt (ENFORCEABLE, still PROSE/STRUCTURAL) — claimed but not guaranteed, soft context-debt.)

### hotam-spec-self
- **purpose** — The methodology modeling itself — Hotam-Spec about itself as a tension graph.
- **goals** — burn down SETTLED-unenforced to zero, atomize all compound check_*, every CLAUDE.md section auto-generated from substrate
- **director** — director
- **path** — `domains/hotam-spec-self/`
- **atoms-count** — 236 SETTLED
- **open actions** — 1 (top: [P0] enforcement-gradient: 41 SETTLED requirements are closeable debt (ENFORCEABLE, still PROSE/STRUCTURAL) — claimed but not guaranteed, soft context-debt.)
<!-- DOMAIN-MAP:END -->
<!-- CONSTITUTION:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Constitution index (business + discipline SETTLED requirements — summary)

> Full id+flag index: `domains/hotam-spec-self/docs/gen/AGENT-CONTEXT.md`. Full claim + WHY + assumptions: `domains/hotam-spec-self/docs/gen/REQUIREMENTS.md` (roster) ·
> one requirement: `hotam req show <id> --domain domains/hotam-spec-self`. enforcement detail: `domains/hotam-spec-self/docs/gen/UNENFORCED.md`.
> Flags: [E] ENFORCED · [S] STRUCTURAL · [P] PROSE.
> Framework internals (112 atoms): `domains/hotam-spec-self/docs/gen/FRAMEWORK-INVARIANTS.md`.

Operator (7) · Substrate / Anchoring (3) · Discipline (14) · Check / Invariant (20) · Framework Self (2) · Lifecycle / Process / Goal (2) · Boot / Glossary / History / Docs (2) · Other (74)
<!-- CONSTITUTION:END -->
<!-- AGENT-MAP:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Agent Map

_(no sub-operators yet)_
<!-- AGENT-MAP:END -->
<!-- CONCEPT-MAP:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Concept Map (§-anchors → defined / enforced / tested)

| § | defined | enforced | tested |
|---|---------|----------|--------|
| **§Agent** | _(not yet mapped)_ | 3 checks | 4 tests |
| **§Assumption** | `internal/ontology/assumption.go` | 1 checks | 4 tests |
| **§Atoms** | _(not yet mapped)_ | _(none)_ | 1 tests |
| **§Attention** | _(not yet implemented)_ | _(none)_ | 3 tests |
| **§Axis** | `internal/ontology/axis.go` | 1 checks | 3 tests |
| **§Closure** | _(not yet implemented)_ | 2 checks | 3 tests |
| **§Conflict** | `internal/ontology/conflict.go` | 17 checks | 7 tests |
| **§Conscience** | _(not yet mapped)_ | _(none)_ | 2 tests |
| **§Constitution** | _(not yet mapped)_ | _(none)_ | 3 tests |
| **§Context** | _(not yet mapped)_ | _(none)_ | 1 tests |
| **§ContextBudget** | _(not yet mapped)_ | 1 checks | 3 tests |
| **§Domain** | _(not yet implemented)_ | 7 checks | 7 tests |
| **§Entity** | `internal/ontology/entity.go` | 12 checks | 6 tests |
| **§Glossary** | `internal/generator/glossary_terms_data.go` | 1 checks | 3 tests |
| **§Goal** | _(not yet mapped)_ | 2 checks | 2 tests |
| **§Graph** | `internal/ontology/graph.go` | _(none)_ | 4 tests |
| **§Invariants** | `internal/diagnose` | 47 checks | 29 tests |
| **§Lifecycle** | `internal/ontology/lifecycle.go` | 10 checks | 8 tests |
| **§Loop** | _(not yet mapped)_ | _(none)_ | _(none)_ |
| **§Operator** | `internal/ontology/operator.go` | 2 checks | 5 tests |
| **§Process** | `internal/ontology/process.go` | 4 checks | 4 tests |
| **§Proposal** | `internal/proposal` | 1 checks | 7 tests |
| **§Reflection** | _(not yet implemented)_ | _(none)_ | 2 tests |
| **§Requirement** | `internal/ontology/requirement.go` | 13 checks | 9 tests |
| **§Scope** | _(not yet implemented)_ | 1 checks | 1 tests |
| **§Signoff** | `internal/ontology/signoff.go` | 2 checks | 2 tests |
| **§Stakeholder** | `internal/ontology/stakeholder.go` | 2 checks | 6 tests |
| **§Tick** | _(not yet mapped)_ | _(none)_ | 1 tests |
| **§Ticket** | _(not yet mapped)_ | _(none)_ | _(none)_ |
<!-- CONCEPT-MAP:END -->
<!-- RECENTLY-REJECTED:BEGIN -->
<!-- (generated by `go run ./cmd/hotam gen-spec` — do not hand-edit) -->

### Recently rejected (anti-relitigation — summary)

Before proposing an architectural change, check this isn't already REJECTED with a REPLACES successor — cite the replacement instead of re-deriving. Full list: `docs/gen/HISTORY.md`.

**REJECTED (REPLACES known)** (41) — R-active-loop-playbooks · R-agent-has-own-crystal · R-agent-imports-framework

_(showing 3 of 41, alphabetical by id — full history + WHY: docs/gen/HISTORY.md, `hotam req show <id>`)_
<!-- RECENTLY-REJECTED:END -->

<!-- Anything you write below this line survives every regeneration verbatim. Use this space for durable notes, reminders, or context that the generator should never touch. -->
