"""Test fixtures for tensio.

These modules contain EXAMPLE business content — Stakeholders, Assumptions,
Requirements, Conflicts, Axes — used only by tests (and by the `--demo` flag of
the tools). They live OUTSIDE the framework package so that `src/tensio/*.py`
stays content-free: a blank kit any domain populates via `spec/content/`.

Importing a fixture is the EXPLICIT opt-in to demo data.
"""
